# Security

CASOPS uses project-root confinement, fixed Git argument arrays, ignored quarantine storage, no installers, no execution of downloaded code, and static scans for sensitive files or installer patterns. Gemini and Gemini CLI are explicitly prohibited source and output ecosystems. Credentialed or broad-access integrations require human approval.
