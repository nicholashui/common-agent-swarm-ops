# Architecture

Manifest validation selects permitted Git references. Root-confined helpers mediate writes. The downloader records metadata without executing source code. Static audit and security stages inform human curation. Two deterministic adapters render approved local inputs; SDD gates validate traceable lifecycle artifacts.
