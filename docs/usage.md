# Usage

Use `sources:download --dry-run` to validate selected references without networking. A normal download performs only Git clone/fetch/pull and metadata inspection. Run audit before any human-approved curation, then use sync dry-run and sync check.
