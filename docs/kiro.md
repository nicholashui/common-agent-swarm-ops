# Kiro

Sync maps shared rules to `.kiro/steering/`, approved hook definitions to `.kiro/hooks/`, and minimal project-local MCP data to `.kiro/mcp.json`. Settings are project-local. Generated files have a managed header and are reproduced by `npm run sync`.
