# Claude Code

Sync maps rules to `CLAUDE.md`, approved skills to `.claude/skills/`, a review command to `.claude/commands/`, a reviewer definition to `.claude/agents/`, and project settings to `.claude/settings.json`. Hook and MCP discussions are documentation only; no Claude hook or MCP configuration is generated.
