# Sync

The target allowlist is Kiro and Claude Code only. Use dry-run to preview files without mutation and `sync:check` to find missing or stale managed files. Correct drift by reviewing user-owned content, then rerunning sync. Outputs are deterministic and local; manual backups are recommended before replacing user changes.
