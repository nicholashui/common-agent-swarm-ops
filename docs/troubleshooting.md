# Troubleshooting

If doctor fails, use Node 20+, Git, and the repository root. If sync check reports drift, run dry-run, inspect planned allowlisted paths, then rerun sync. If a source fails, inspect the lock and audit; never run upstream setup scripts to repair it.
