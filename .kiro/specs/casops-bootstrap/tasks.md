# Tasks

- [x] 1. Implement source manifests and confined filesystem helpers (REQ-1).
- [x] 2. Implement Git-only dry-run-aware downloader and audit (REQ-1, REQ-3).
- [x] 3. Implement Kiro and Claude Code adapters and scoped sync (REQ-2).
- [x] 4. Implement SDD templates, gate, tracked hook, and CI templates (REQ-4).
- [x] 5. Add static security checks and tests (REQ-1, REQ-2, REQ-5).
- [x] 6. Curate user-approved ECC documentation-only skills with attribution and scoped Kiro/Claude sync (REQ-6).

## Validation
- [ ] `npm test`
- [ ] `npm run doctor`
- [ ] `npm run security`
- [ ] `npm run sync -- --dry-run`
- [ ] `npm run sync:check`
- [ ] `npm run sdd:check`
