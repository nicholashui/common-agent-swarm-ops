# Claude Code Sync Workflow

Run `npm run sync -- --target claude-code --dry-run`, review only `CLAUDE.md`, settings, skills, commands, and agents, then run sync and `sync:check`. Hook and MCP information is documentation only.
