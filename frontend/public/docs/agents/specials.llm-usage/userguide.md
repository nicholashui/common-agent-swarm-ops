# Llm Usage — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.llm-usage/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Llm Usage** (`specials.llm-usage`) is a Specials pack capability on CASOPS. In plain terms: Owns the specials-domain llm usage design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Llm Usage |
| **Pack agent id** | `specials.llm-usage` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.llm-usage-prompt` / `spagent.llm-usage-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: Owns the specials-domain llm usage design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Owns the specials-domain llm usage design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
- **Self-quality bar:** Traceable outputs for llm usage policy; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent llm usage policy outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** LLM usage policy pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.llm-usage-rubric` (inert identifier).
- Prompt reference: `spagent.llm-usage-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.llm-usage-input"],"outputs":["spagent.llm-usage-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

Owns the specials-domain llm usage design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in llm usage policy, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.llm-usage-input']`
- **Host critique edge outputs:** `['spagent.llm-usage-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** Model selection, budgets, and safe LLM usage patterns must be governed.
- **Capability family:** LLM usage policy
- **Typical upstream:** Controller / planner requests.
- **Typical downstream:** All LLM-calling agents.
- **Concrete mission fragment:** Owns the specials-domain llm usage design outcome as a draft, data-only agent representation.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Llm Usage*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in llm usage policy, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.llm-usage-input'] |
| **Host edge outputs** | ['spagent.llm-usage-output'] |

### Specials lane placement

- **Family:** LLM usage policy
- **Upstream:** Controller / planner requests.
- **Downstream:** All LLM-calling agents.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **LLM usage policy pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.llm-usage` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.llm-usage/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.llm-usage-rubric` against: Traceable outputs for llm usage policy; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.llm-usage-prompt` with responsibility: Owns the specials-domain llm usage design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.llm-usage-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: LLM usage policy pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent llm usage policy outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for llm usage policy; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.llm-usage` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/llm_usage.md` - Source SHA-256: `6bac2d5882382f1a85920b5854879cf295edb86fa5a48cfdbcd43c72c6fb97fd` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.llm-usage",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/llm_usage.md"
  ],
  "source_sha256": "6bac2d5882382f1a85920b5854879cf295edb86fa5a48cfdbcd43c72c6fb97fd",
  "configuration_sha256": "730b3e6005a6d6a9b94647bb151b799f58b3841023e953e14cf2ef7159304782",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:45.952022Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/llm_usage.md` - Design source SHA-256 (at generation): `6bac2d5882382f1a85920b5854879cf295edb86fa5a48cfdbcd43c72c6fb97fd` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/llm_usage_functional_specification.md` | # Build Central LLM API Usage & Cost Dashboard App ## Project Name Suggestion **LLMUsageHub** or **MultiLLM Dashboard** or **API Cost Central** or **LLM Spend Tracker** ## 1. (score 5) |
| `study/llm_usage_functional_specification_hk.md` | # 建立中央 LLM API 使用和成本儀表板應用程序 ## 項目名稱建議 **LLMUsageHub** 或 **MultiLLM 儀表板** 或 **API 成本中心** 或 **LLM 支出追蹤器** ## 一、專案概況 建立一個**網頁應用程式**，提供**單一中央視圖**，用於追蹤使用者的**所有** LLM API 帳户的使用情況、成本、餘額、支出和代幣消耗。 用户目前擁有以下帳户： - x.ai（Grok API） - Poe - MiniMax - 基米（Moonshot AI） - OpenRouter ……以及許多其他人。 該應用程式應該讓用户添加一次 API 金鑰，然後查看**在一個漂亮的儀表板中聚合的所有內容** - 每月總支出、剩餘積分、每個提供者的細分、圖表、趨勢、警報等。 **參考/靈感**： 受到 **[cc-switch](https://github.com/farion1231/cc-switch)** （用於管理 Claude Code / Codex / Gemini CLI 的 LLM 提供者的流行桌面工具）的啟發。此 Web 應用程式**純粹專注於跨直接個人 API 金鑰的使用/成本分析**，作為 cc-switch 的僅使用伴侶，但作為 Web 應用程式。 ## 2. (score 5) |

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/llm_usage.md`

**Outline (for navigation):**

- Build Central LLM API Usage & Cost Dashboard App
- Project Name Suggestion
- 1. Project Overview
- 2. Core Goals
- 3. Key Features (Must-Have)
- Provider Management
- Usage & Balance Fetching
- Dashboard UI
- Cost Calculation
- Data Persistence & Export
- Security & UX
- Nice-to-Have (Phase 2)
- 4. Supported Providers (List as Many as Possible)
- 5. Technical Stack
- Backend
- API Design (OpenAPI)
- Frontend
- Architecture
- 6. Development Phases (Suggested)
- 7. Deliverables

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

#### Build Central LLM API Usage & Cost Dashboard App

#### Project Name Suggestion
**LLMUsageHub** or **MultiLLM Dashboard** or **API Cost Central** or **LLM Spend Tracker**

#### 1. Project Overview
Create a **web application** that provides a **single central view** for tracking usage, costs, balances, spending, and token consumption across **all** of the user's LLM API accounts.

The user currently has accounts with:  
- x.ai (Grok API)  
- Poe  
- MiniMax  
- Kimi (Moonshot AI)  
- OpenRouter  
...and many others.

The app should let the user add their API keys once and see **everything aggregated in one beautiful dashboard** — total monthly spend, remaining credits, per-provider breakdowns, charts, trends, alerts, etc.

**Reference / Inspiration**:
Inspired by **[cc-switch](https://github.com/farion1231/cc-switch)** (the popular desktop tool for managing LLM providers for Claude Code / Codex / Gemini CLI). This web app is **purely focused on usage/cost analytics** across direct personal API keys, serving as a usage-only companion to cc-switch but as a web application.

#### 2. Core Goals
- One unified place to monitor **all** LLM spending and usage.
- Secure, local-only storage of API keys (never sent to any server).
- Automatic or on-demand fetching of usage/billing data.
- Historical tracking + visualizations.
- Extremely extensible — easy to add new providers.
- Beautiful, modern UI similar to cc-switch.

#### 3. Key Features (Must-Have)

#### Provider Management
- Add / edit / remove accounts with: name, provider type (preset), API key, base URL (for custom endpoints), notes.
- Pre-built **presets** for as many providers as possible (see section 4).
- Support multiple accounts per provider.
- One-click “Refresh All” and individual refresh buttons.

#### Usage & Balance Fetching
- Prefer **official APIs** where available (e.g. `/usage`, `/billing`, `/balance`, `/v1/token_plan/remains`, etc.).
- Fallback options:
  - Manual entry of current usage/balance.
  - Web dashboard scraping (using Playwright if needed, last resort).
- Background auto-refresh (configurable interval) + manual refresh.
- Store full history snapshots in local DB.

#### Dashboard UI
- **Overview page**:
  - Total estimated USD spend (today / this month / all time).
  - Total remaining credits/balance (normalized where possible).
  - Number of active providers + quick status.
- **Provider cards** (grid or list):
  - Name + logo (if available).
  - Current balance / remaining credits.
  - Spend this month + trend indicator.
  - Last updated timestamp.
- **Charts**:
  - Spending trend (line chart — daily/weekly).
  - Cost breakdown by provider (pie).
  - Token usage by model (bar).
  - Usage heatmap or calendar view.
- **Detailed tables**:
  - Per-provider usage history.
  - Model-level breakdown.
- **Alerts**:
  - Low balance warnings (configurable thresholds).
  - High daily spend notifications.

#### Cost Calculation
- Built-in pricing tables for major models (input/output tokens → USD).
- Allow user to override pricing per model.
- Show estimated USD even when provider only reports tokens.

#### Data Persistence & Export
- Local **SQLite** database for all historical usage snapshots.
- Export full data as CSV or JSON.

#### Security & UX
- API keys stored **encrypted** locally (Fernet symmetric encryption).
- Dark/light theme (default dark, matching modern AI tools).
- Browser-based UI accessible from localhost.
- Fully offline-first after initial setup.
- Responsive, clean, professional UI.

#### Nice-to-Have (Phase 2)
- AI-powered insights (“You spent 68% on Kimi this month — consider switching heavy tasks to Groq”).
- Import/export configuration (including possible cc-switch import).
- Per-model cost forecasting.
- Optional proxy/router mode (like LiteLLM or cc-switch) so the app can also log usage from actual API calls.

#### 4. Supported Providers (List as Many as Possible)
The app must ship with **pre-built presets** (fetch logic + pricing) for **as many providers as possible**. Start with user-mentioned ones, then expand.

**High Priority (User’s Current Providers)**
- xAI (Grok API) — console.x.ai usage / billing endpoints
- Poe.com — usage/points_history and current_balance endpoints
- OpenRouter — account usage API
- MiniMax — token plan remains and usage endpoints
- Kimi (Moonshot AI) — platform.moonshot.ai usage/balance API

**Other Major Providers (Include Full Presets)**
- OpenAI
- Anthropic (Claude)
- Google Gemini / Vertex AI
- Groq
- Mistral

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.llm-usage.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.llm-usage",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.llm-usage-prompt",
  "rubric_reference": "spagent.llm-usage-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.llm-usage-input"
    ],
    "outputs": [
      "spagent.llm-usage-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.llm-usage","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.llm-usage-prompt","rubric_reference":"spagent.llm-usage-rubric","critique_edges":{"inputs":["spagent.llm-usage-input"],"outputs":["spagent.llm-usage-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain llm usage design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
The user currently has accounts with:  
- x.ai (Grok API)  
- Poe  
- MiniMax  
- Kimi (Moonshot AI)  
- OpenRouter  
...and many others. The app should let the user add their API keys once and see **everything aggregated in one beautiful dashboard** — total monthly spend, remaining credits, per-provider breakdowns, charts, trends, alerts, etc.

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.llm-usage/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Llm Usage**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
