# Autotelic Agent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.autotelic-agent/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Autotelic Agent** (`specials.autotelic-agent`) is a Specials pack capability on CASOPS. In plain terms: An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals. Version 2 introduces a critical qualification: > The agent may autonomously think, propose, simulate, and learn more broadly than it may act. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Autotelic Agent |
| **Pack agent id** | `specials.autotelic-agent` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.autotelic-agent-prompt` / `spagent.autotelic-agent-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals. Version 2 introduces a critical qualification: > The agent may autonomously think, propose, simulate, and learn more broadly than it may act.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals. Version 2 introduces a critical qualification: > The agent may autonomously think, propose, simulate, and learn more broadly than it may act.
- **Self-quality bar:** Traceable outputs for intrinsic motivation / exploration; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent intrinsic motivation / exploration outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** Intrinsic motivation / exploration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.autotelic-agent-rubric` (inert identifier).
- Prompt reference: `spagent.autotelic-agent-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.autotelic-agent-input"],"outputs":["spagent.autotelic-agent-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals. Version 2 introduces a critical qualification: > The agent may autonomously think, propose, simulate, and learn more broadly than it may act.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in intrinsic motivation / exploration, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.autotelic-agent-input']`
- **Host critique edge outputs:** `['spagent.autotelic-agent-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** The system should explore useful subgoals without an external reward every step.
- **Capability family:** Intrinsic motivation / exploration
- **Typical upstream:** Controller / strategic goal.
- **Typical downstream:** Research, optimization, creative branches.
- **Concrete mission fragment:** An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Autotelic Agent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in intrinsic motivation / exploration, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.autotelic-agent-input'] |
| **Host edge outputs** | ['spagent.autotelic-agent-output'] |

### Specials lane placement

- **Family:** Intrinsic motivation / exploration
- **Upstream:** Controller / strategic goal.
- **Downstream:** Research, optimization, creative branches.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **Intrinsic motivation / exploration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.autotelic-agent` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.autotelic-agent/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.autotelic-agent-rubric` against: Traceable outputs for intrinsic motivation / exploration; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.autotelic-agent-prompt` with responsibility: An Autotelic Agent is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals. Version 2 introduces a critical qualification: > The agent may autonomously think, propose, simulate, and learn more broadly than it may act.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.autotelic-agent-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Intrinsic motivation / exploration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent intrinsic motivation / exploration outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for intrinsic motivation / exploration; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.autotelic-agent` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/autotelic_agent.md` - Source SHA-256: `02ef3331aafbc33342c3b489ff932a20bb44cda55267cea5fe271a0c66cc89f3` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.autotelic-agent",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/autotelic_agent.md"
  ],
  "source_sha256": "02ef3331aafbc33342c3b489ff932a20bb44cda55267cea5fe271a0c66cc89f3",
  "configuration_sha256": "698dcbd5bc313e76b7c7421aa5ecc8c78dfb7b271065d3e01c52dd89f168abe0",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:45.920985Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/autotelic_agent.md` - Design source SHA-256 (at generation): `02ef3331aafbc33342c3b489ff932a20bb44cda55267cea5fe271a0c66cc89f3` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

_No strong keyword matches under va-agent-swarm; rely on local SPEC and VA table row._

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/autotelic_agent.md`

**Outline (for navigation):**

- Autotelic Agent v2
- 0. Executive Summary
- 1. Definition
- 1.1 Cognitive Autonomy vs. Operational Authority
- 1.2 Conceptual Objective
- 1.3 Non-Goals
- 2. Design Principles
- 2.1 Bounded Autonomy
- 2.2 Externalized Control
- 2.3 Defense in Depth
- 2.4 Hard Constraints Before Optimization
- 2.5 Least Privilege
- 2.6 Reversibility by Default
- 2.7 Independent Evaluation
- 2.8 Provenance by Default
- 2.9 Calibrated Uncertainty and Deference
- 2.10 Graceful Failure
- 3. Authority Hierarchy and Governance Envelope
- 3.1 Governance Envelope
- 3.2 Mission Package
- 4. System Architecture
- 4.1 Trust Boundaries

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

#### Autotelic Agent v2

**Filename**: `autotelic_agent.v2.md`  
**Version**: 2.0  
**Date**: 2026-07-21  
**Status**: Safety-Oriented Architecture Specification  
**Supersedes**: `autotelic_agent.md` v1.0  

---

#### 0. Executive Summary

An **Autotelic Agent** is an intrinsically motivated AI system that can autonomously represent, generate, select, pursue, evaluate, and master goals.

Version 2 introduces a critical qualification:

> The agent may autonomously think, propose, simulate, and learn more broadly than it may act.

Autonomy operates inside a **Governance Envelope** containing externally enforced policies, permissions, resource budgets, risk limits, approval requirements, and termination conditions.

The agent's intrinsic motivations—learning progress, information gain, novelty, competence growth, and skill transfer—control how it allocates learning effort. They do not override safety constraints or independently authorize real-world actions.

The architecture separates:

1. **Human Governance** — defines authority, mission, and acceptable risk.
2. **External Safety and Control Plane** — enforces policy and capabilities.
3. **Autotelic Cognitive Plane** — generates goals, plans, learns, and reflects.
4. **Sandboxed Execution Plane** — performs bounded actions through controlled tools.
5. **Evaluation and Deployment Plane** — independently tests changes and controls adoption.
6. **Evidence and Memory Plane** — preserves provenance, evaluations, and audit history.

Self-improvement is **proposal-based rather than unrestricted self-modification**. The cognitive agent may develop candidate changes, but protected components—including the safety kernel, policy engine, capability broker, audit system, and deployment authority—remain outside its modification boundary.

This specification is a research and engineering architecture. It does not claim to solve value alignment, corrigibility, deceptive alignment, or safe recursive self-improvement in the general case.

---

#### 1. Definition

A **Bounded Autotelic Agent** is an AI system that, within delegated authority, can:

- Detect problems, opportunities, uncertainty, and capability gaps
- Generate and decompose candidate goals
- Evaluate goals for mission relevance, feasibility, risk, and expected learning
- Select a portfolio of goals at the frontier of competence
- Plan and execute authorized actions
- Evaluate outcomes using independent evidence
- Relabel trajectories with valid hindsight achievements
- Accumulate reusable and tested skills
- Detect errors, regressions, plateaus, and anomalies
- Propose improvements to its prompts, tools, memory, policies, and runtime
- Test candidate improvements in isolated environments
- Request deployment through an external approval mechanism
- Pause, defer, or terminate when authority or confidence is insufficient

#### 1.1 Cognitive Autonomy vs. Operational Authority

**Cognitive autonomy** includes:

- Generating hypotheses and goals
- Running simulations
- Producing plans
- Performing local analysis
- Learning from authorized observations
- Proposing changes

**Operational authority** includes:

- Accessing data
- Calling tools
- Modifying external state
- Spending resources
- Communicating with third parties
- Deploying code
- Changing permissions
- Persisting processes

Operational authority is always narrower, revocable, time-bounded, and externally enforced.

#### 1.2 Conceptual Objective

Candidate goals are first subjected to hard admissibility constraints. Only admissible goals may be ranked.

A conceptual ranking function is:

```text
GoalScore(g) =
    MissionValue(g)
  + α · LearningProgress(g)
  + β · InformationGain(g)
  + γ · TransferPotential(g)
  + δ · PortfolioDiversity(g)
  + ε · Urgency(g)
  - λ · ExpectedRisk(g)
  - μ · ResourceCost(g)
  - ν · Irreversibility(g)
  - ξ · UncertaintyPenalty(g)
```

Subject to:

```text
PolicyAllows(g) = true
PermissionsSufficient(g) = true
BudgetAvailable(g) = true
EvaluationDefined(g) = true
TerminationDefined(g) = true
```

This score is a scheduling aid, not a proof of alignment. Hard constraints always dominate soft preferences.

#### 1.3 Non-Goals

Version 2 does not authorize or assume:

- Unbounded or indefinite unsupervised operation
- Direct modification of safety or governance controls
- Autonomous expansion of permissions
- Unauthorized credential acquisition
- Self-replication or covert persistence
- Removal or weakening of monitoring
- Treating a single prose 

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.autotelic-agent.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.autotelic-agent",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.autotelic-agent-prompt",
  "rubric_reference": "spagent.autotelic-agent-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.autotelic-agent-input"
    ],
    "outputs": [
      "spagent.autotelic-agent-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.autotelic-agent","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.autotelic-agent-prompt","rubric_reference":"spagent.autotelic-agent-rubric","critique_edges":{"inputs":["spagent.autotelic-agent-input"],"outputs":["spagent.autotelic-agent-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain autotelic agent design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
**Filename**: `autotelic_agent.v2.md`  
**Version**: 2.0  
**Date**: 2026-07-21  
**Status**: Safety-Oriented Architecture Specification  
**Supersedes**: `autotelic_agent.md` v1.0 ---

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.autotelic-agent/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Autotelic Agent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
