# Aesthetics Agent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.aesthetics-agent/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Aesthetics Agent** (`specials.aesthetics-agent`) is a Specials pack capability on CASOPS. In plain terms: The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents. It serves three distinct, composable functions: 1. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Aesthetics Agent |
| **Pack agent id** | `specials.aesthetics-agent` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.aesthetics-agent-prompt` / `spagent.aesthetics-agent-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents. It serves three distinct, composable functions: 1.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents. It serves three distinct, composable functions: 1.
- **Self-quality bar:** Traceable outputs for aesthetic judgment; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent aesthetic judgment outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** Aesthetic judgment pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.aesthetics-agent-rubric` (inert identifier).
- Prompt reference: `spagent.aesthetics-agent-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.aesthetics-agent-input"],"outputs":["spagent.aesthetics-agent-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents. It serves three distinct, composable functions: 1.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in aesthetic judgment, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.aesthetics-agent-input']`
- **Host critique edge outputs:** `['spagent.aesthetics-agent-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** Taste, style coherence, and visual/sonic quality must be judged.
- **Capability family:** Aesthetic judgment
- **Typical upstream:** Creative outputs.
- **Typical downstream:** Creative director / craft agents; optimization.
- **Concrete mission fragment:** The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Aesthetics Agent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in aesthetic judgment, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.aesthetics-agent-input'] |
| **Host edge outputs** | ['spagent.aesthetics-agent-output'] |

### Specials lane placement

- **Family:** Aesthetic judgment
- **Upstream:** Creative outputs.
- **Downstream:** Creative director / craft agents; optimization.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **Aesthetic judgment pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.aesthetics-agent` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.aesthetics-agent/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.aesthetics-agent-rubric` against: Traceable outputs for aesthetic judgment; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.aesthetics-agent-prompt` with responsibility: The Aesthetics Agent is the swarm's computational embodiment of "artiste sense." It does not replace human taste — it amplifies, encodes, and propagates it at superhuman speed across 114 agents. It serves three distinct, composable functions: 1.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.aesthetics-agent-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Aesthetic judgment pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent aesthetic judgment outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for aesthetic judgment; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.aesthetics-agent` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/aesthetics_agent.md` - Source SHA-256: `7d5b36b2dbc4e70664e9213d5a89d057935e54a1c3f6ab8cbb639fee5ab0f8d1` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.aesthetics-agent",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/aesthetics_agent.md"
  ],
  "source_sha256": "7d5b36b2dbc4e70664e9213d5a89d057935e54a1c3f6ab8cbb639fee5ab0f8d1",
  "configuration_sha256": "0a6ec24af4f79a6640f921c7aeb020dbe9d4e01783b36a47ae14f1ac5034da7c",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:45.905948Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/aesthetics_agent.md` - Design source SHA-256 (at generation): `7d5b36b2dbc4e70664e9213d5a89d057935e54a1c3f6ab8cbb639fee5ab0f8d1` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/aesthetics_agent_functional_specification.md` | **Comprehensive Functional Specification: Aesthetics Agent — Computational "Artiste Sense" Critic & Alignment Service for the VA-Agent-Swarm** **Document Version:** 1.0 (Final – Complete & Exhaustive) **Date:** May 29, 2026 **Authors:** Grok (xAI) + Collaborative Iteration with User Nicholas (nicholas_hui) **Target Audience:** Senior AI Engineering / Coding Agents (for immediate implementation) **Purpose:** This is the **definitive, production-grade specification** for building the **Aesthetics Agent** — a stateful, multimodal evaluation-and-alignment service that operationalizes a simulated (score 7) |
| `study/aesthetics_agent_functional_specification_hk.md` | **綜合功能規格：美學代理 — 為 VA-Agent-Swarm 提供運算「藝術家感知 (Artiste Sense)」的評審與對齊服務** **文件版本：** 1.0（最終版 - 完整且詳盡） **日期：** 2026 年 5 月 29 日 **作者：** Grok (xAI) + 與使用者 Nicholas (nicholas_hui) 的協同迭代 **目標受眾：** 高級人工智慧工程/編碼代理（立即實施） **目的：** 這是用於構建 **美學代理 (Aesthetics Agent)** 的 **最終生產級規範** — 一個有狀態的、多模態的評估與對齊服務，為整個 VA-Agent-Swarm 操作化一種模擬的 **「藝術家感知」**（藝術/美學直覺）。它是對 [`aesthetics_agents.md`](./aesthetics_agents.md) 的深度重新思考：該文件是一份 *方法綜述*，而本文件是一份 *可建構的代理契約*。它將「教 AI 藝術感知」從單一圖像評分器重新建構為一個 **共享的美學神經系統** — 一個每個生成代理都會諮詢的評審、一個每個微調迴圈都信任的獎勵訊號，以及一個將導演、品牌或藝術家的品味貫穿整條生產線的個人化引擎。 --- ### 1. (score 7) |

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/aesthetics_agent.md`

**Outline (for navigation):**

- 1. Executive Summary
- 2. Background: From "Artiste Sense" to Computational Aesthetics
- 3. The Deep Rethink — Five Reframings
- 4. Formal Aesthetic Model
- 5. Architecture

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

**Comprehensive Functional Specification: Aesthetics Agent — Computational "Artiste Sense" Critic & Alignment Service for the VA-Agent-Swarm**

**Document Version:** 1.0 (Final – Complete & Exhaustive)
**Date:** May 29, 2026
**Authors:** Grok (xAI) + Collaborative Iteration with User Nicholas (nicholas_hui)
**Target Audience:** Senior AI Engineering / Coding Agents (for immediate implementation)
**Purpose:** This is the **definitive, production-grade specification** for building the **Aesthetics Agent** — a stateful, multimodal evaluation-and-alignment service that operationalizes a simulated **"artiste sense"** (artistic/aesthetic intuition) for the entire VA-Agent-Swarm. It is the deep rethink of [`aesthetics_agents.md`](./aesthetics_agents.md): where that document is a *survey of methods*, this is a *buildable agent contract*. It reframes "teach AI artistic sense" from a single image scorer into a **shared aesthetic nervous system** — a critic that every generative agent consults, a reward signal that every fine-tuning loop trusts, and a personalization engine that carries a director's, brand's, or artist's taste across the whole pipeline.

---

#### 1. Executive Summary

The **Aesthetics Agent** is the swarm's computational embodiment of "artiste sense." It does not *replace* human taste — it **amplifies, encodes, and propagates** it at superhuman speed across 114 agents.

It serves three distinct, composable functions:

1. **The Critic (Perceive).** A multimodal, multi-head evaluator that scores any visual artifact — single frame, image, or full video clip — against a decomposed aesthetic rubric (composition, color harmony, lighting, depth, emotional resonance, technical quality, style fidelity, novelty). This is the swarm's shared "eye."
2. **The Aligner (Refine).** A preference-and-reward service that turns the Critic's judgments into actionable feedback and training signal — driving self-refinement loops, preference optimization (DPO/RLHF/RLAIF), and prompt steering for `PromptEngineerAgent`, `CinematographerAgent`, `ColoristAgent`, and peers.
3. **The Taste-Keeper (Personalize).** A profile store that captures *whose* aesthetic governs a project — a director's lookbook, a brand's guidelines, an artist's portfolio, an audience cohort's preferences — and conditions all scoring and alignment on that profile.

**Why this is a "deep rethink" and not a wrapper:**

| The naive guide says... | The Aesthetics Agent does... |
|---|---|
| "Train an aesthetic scorer (e.g., NIMA, LAION)." | Treats a single scalar score as *insufficient and dangerous*. Decomposes aesthetics into auditable sub-attributes + a temporal track for video, with calibrated uncertainty. |
| "Use the critic as a reward to fine-tune the generator." | Wraps every reward use in **anti-reward-hacking** guardrails (reward variance monitoring, ensemble disagreement, OOD detection, KL anchoring). |
| "Personalize on one artist's ratings." | Makes taste a **first-class, versioned, consent-governed profile** that flows through the swarm's handoff contract and critique bus. |
| Operates on still images. | Is **video-native first**: temporal coherence, motion aesthetics, cut rhythm, and per-shot-vs-sequence scoring. |
| Aesthetic sense = beauty prediction. | Aesthetic sense = beauty **+ intent fidelity + emotional target + brand/style fit + novelty**, gated so high scores never come from generic "pretty slop." |

The result is a single agent that other agents *cannot do their job without*: it is referenced by `agents.md` entries #6, #10, #14, #15, #16, #39, #46, #49 and others as "aesthetic regressor / CLIP-based aesthetic scoring," and this document is its authoritative definition.

---

#### 2. Background: From "Artiste Sense" to Computational Aesthetics

The source document [`aesthetics_agents.md`](./aesthetics_agents.md) defines **"artiste sense"** as the intuitive, perceptual, and expressive sensitivity artists develop — an "eye" for composition, color harmony, rhythm, proportion, light/shadow, depth, and emotional resonance; a structural (3D) way of seeing; a psychological drive to express; and a grounded, iterative creative practice.

Its central thesis, which this spec adopts wholesale:

> AI cannot possess *genuine* lived artistic sense (no consciousness, no emotion), but it can develop a **sophisticated simulated version** through (a) data-driven evaluators trained on human aesthetic judgments, (b) preference-alignment feedback loops, and (c) computat

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.aesthetics-agent.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.aesthetics-agent",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.aesthetics-agent-prompt",
  "rubric_reference": "spagent.aesthetics-agent-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.aesthetics-agent-input"
    ],
    "outputs": [
      "spagent.aesthetics-agent-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.aesthetics-agent","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.aesthetics-agent-prompt","rubric_reference":"spagent.aesthetics-agent-rubric","critique_edges":{"inputs":["spagent.aesthetics-agent-input"],"outputs":["spagent.aesthetics-agent-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain aesthetics agent design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
This is the **definitive, production-grade specification** for building the **Aesthetics Agent** — a stateful, multimodal evaluation-and-alignment service that operationalizes a simulated **"artiste sense"** (artistic/aesthetic intuition) for the entire VA-Agent-Swarm. It is the deep rethink of [`aesthetics_agents.md`](./aesthetics_agents.md): where that document is a *survey of methods*, this is a *buildable agent contract*. It reframes "teach AI artistic sense" from a single image scorer into a **shared aesthetic nervous system** — a critic that every generative agent consults, a reward signal that every fine-tuning loop trusts, and a personalization engine that carries a director's, brand's, or artist's taste across the whole pipeline.

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.aesthetics-agent/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Aesthetics Agent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
