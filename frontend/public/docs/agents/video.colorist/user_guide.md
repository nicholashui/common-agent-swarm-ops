# ColoristAgent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/video/agents/video.colorist/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**ColoristAgent** (`video.colorist`) is a Video Domain Pack role on CASOPS. In plain terms: Would target audience choose this over human baseline? Treat this guide as the human-readable operator map for catalog review, swarm design, and proposal-driven improvement — not as a runtime activation license.

| | |
|---|---|
| **Display name** | ColoristAgent |
| **Pack agent id** | `video.colorist` |
| **Pack** | `video` |
| **Host role string** | ColoristAgent (VA Domain Pack) |
| **Catalog status** | `registered` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `10` |
| **VA category** | `3-Edit` — Editorial & finishing |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `video.prompt.colorist.v1` / `video.rubric.colorist.v1` |
| **Previous common id** | `video.colorist` |
| **Generic lineage** | `generic-swarm-ops/business/video/agents/video.colorist` |

## 2. Why this role exists

Human video crews already solve this problem under time pressure: Would target audience choose this over human baseline? The agent form exists so the multi-agent system can **encode that craft continuously**, apply it on every handoff, and escalate only the residual risk to humans.

Its knowledge base is distilled from: **Pairwise comparison: AudienceSim panel (≥200 simulated personas + ≥20 HiTL samples). Win rate ≥50% (parity) or ≥55% (surpass).** — historical design sources, not live network calls on this host.

The design ambition (surpass-human signal) is: *11.* Use that as the north-star metric when you write evals or proposals later.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Would target audience choose this over human baseline?
- **Self-quality bar:** ΔE drift <2; skin-tone IT8 alignment; mood vector match
- **Ambition signal (design-time):** 11
- **Architecture pattern (design-time):** Self-Refine + tool-use (colorimeter validation)

### Host quality & critique binding

- Prompt reference: `video.prompt.colorist.v1`
- Rubric reference: `video.rubric.colorist.v1`
- Critique edges: `{"inputs": ["video.critic"], "outputs": ["video.judge"]}`
- Max refinement: `3`
- VA table quality criteria retained under Provenance and Identity surface above.

### Inputs → outputs → acceptance

- Inputs: local pack artifacts and typed handoffs.
- Outputs: reviewable video-domain deliverables with acceptance criteria.
- Acceptance: host policy plus local SPEC criteria; no external repository required.

## 4. Scope: owns, shares, and refuses

### Owns

Would target audience choose this over human baseline?

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** **Surpass-Human Signal**
- **Is qualified to comment on:** Pre-registered proof the agent exceeds a credentialed professional
- **Host critique edge inputs:** `['video.critic']`
- **Host critique edge outputs:** `['video.judge']`
- **Max refinement count:** `3`

### Refuses / escalates

- Fail-closed: no provider activation, no network, no credentials from design text.
- `production_activation_requested` remains false unless a separate human gate changes it.
- Escalates legal, safety, rights, and release decisions to required human gates.
- Critique lead / judge defaults use VA IDs `video.critic` and `video.judge`.

## 5. When to involve this agent

- **Call this agent when** the work product needs a decision or artifact owned by *ColoristAgent*: Would target audience choose this over human baseline?
- **Pipeline phase:** Editorial & finishing. Post-production editorial, color, and picture finish.
- **Do not call it for** generic orchestration, budget greenlight, or final legal sign-off unless those duties are explicitly in its responsibility — use producer/legal/compliance peers instead.
- **Human override:** any release-blocking safety, rights, or accessibility dispute escalates to a human gate even if the agent would auto-pass in a future active runtime.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *ColoristAgent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | **Surpass-Human Signal** |
| **Outbound commentary** | Pre-registered proof the agent exceeds a credentialed professional |
| **Host edge inputs** | ['video.critic'] |
| **Host edge outputs** | ['video.judge'] |

## 7. Where it sits in production

### 7.1 Human production workflow

Post-production editorial, color, and picture finish.

**Typical handoff artifacts:** Cuts, timelines, grades, continuity notes, QC tickets.

**Mentions in local human-workflow excerpt:**

- 5 — Aesthetic / vibe videos (lo-fi, cyberpunk, nature, retro) — 10–60s — Instagram, TikTok — Cinematographer or AI Generator Operator, Colorist, Music Curator, Editor
- 4 — Before & After transformations — 10–30s — Reels, TikTok — Director, DoP, Talent, Editor, Colorist, Compliance Reviewer
- 15 — Founder-story authenticity videos — 60–120s — LinkedIn, web — Interviewer, DoP, Sound Recordist, Editor, Colorist, Brand Strategist

### 7.2 Multi-agent AI workflow

Cut assembly, grade, and picture QC nodes with critique loops against director/audience judges.

Design-time tool notes (non-binding on host): Benchmark dominance; blind Arena preference ≥55%; speed × quality (equal L2 at ≤10% turnaround); lower 90-day defect rate; certification pass; higher novelty at equal quality.

Loop style to emulate when you later implement runtime: **Self-Refine + tool-use (colorimeter validation)**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

**Mentions in local AI-workflow excerpt:**

- 6 — **CinematographerAgent (DoP)** — Lensing, lighting, composition, look — ASC Magazine 1980–present; Deakins forum; *Cinematography: Theory & Practice* (Brown); shot-libraries from Cannes selections — Rule-of-thirds / leading-lines score; exposure histogram in zone; color-temp consistency across shots — Beats ASC peer-juried short reels in blind aesthetic preference — DirectorAgent, ColoristAgen
- 10 — **ColoristAgent** — Final grade; look consistency — ICA course corpora; Stefan Sonnenfeld grading sessions; HPA Award-winning grades — ΔE drift across shots <2; skin-tone IT8 chart alignment; mood vector matches reference — Beats junior colorist in blind preference; matches senior colorist within ΔE budget — DoPAgent, DirectorAgent, AccessibilityAgent (contrast) — DoPAgent (mixed-temp footage
- 61 — **StyleTransferAgent** — Applies named aesthetic (Wes Anderson, A24, cyberpunk, vaporwave, Studio Ghibli, etc.) consistently across shots — Curated style corpora per look; LoRA/seed registries; reference-frame banks — Style-similarity score (CLIP/DINO) ≥0.85 to reference; consistency variance across shots ≤τ — Wins blind preference vs human colorist+grader doing same look — DirectorAgent, Col

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/video.colorist` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/video.colorist/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `2048` / out `1024` / tool requests `0`.
- Design-time tool catalogue (non-activating): Benchmark dominance; blind Arena preference ≥55%; speed × quality (equal L2 at ≤10% turnaround); lower 90-day defect rate; certification pass; higher novelty at equal quality.

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `video.rubric.colorist.v1` against: ΔE drift <2; skin-tone IT8 alignment; mood vector match
2. **Prompt precision** — align `video.prompt.colorist.v1` with responsibility: Would target audience choose this over human baseline?
3. **Critique graph** — ensure inbound `**Surpass-Human Signal**` and host edges `['video.critic']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Self-Refine + tool-use (colorimeter validation)
5. **Eval cases** — write fixed fixtures that would fail if *11* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: ΔE drift <2; skin-tone IT8 alignment; mood vector match |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |
| `sources/excerpts/agents.md` | `historical-excerpt` |
| `sources/excerpts/ai_agent_video_production_workflow.md` | `historical-excerpt` |
| `sources/excerpts/backend_agent_management.md` | `historical-excerpt` |
| `sources/excerpts/human_video_production_workflow.md` | `historical-excerpt` |
| `sources/excerpts/lifes_quiet_redemption_agent_workflow.md` | `historical-excerpt` |
| `sources/excerpts/production_scale_discovery.md` | `historical-excerpt` |
| `sources/excerpts/system_build_plan.md` | `historical-excerpt` |
| `sources/excerpts/SYSTEM_REFERENCE.md` | `historical-excerpt` |
| `sources/excerpts/ui_design.md` | `historical-excerpt` |
| `sources/excerpts/video_remake_enhancement.md` | `historical-excerpt` |
| `sources/generic/video.colorist.SPEC.md` | `historical-generic` |

### 11.2 Mapping note

# Mapping — `video.colorist` - VA/generic pack ID: `video.colorist` - Previous common ID: `video.colorist` - SPEC depth: full generic SPEC body + host runtime binding

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "agent_id": "video.colorist",
  "previous_common_agent_id": "video.colorist",
  "va_taxonomy_aligned": true,
  "generic_source": "generic-swarm-ops/business/video/agents/video.colorist",
  "note": "Agent ID aligned to VA/generic Domain Pack taxonomy. Upstream design is historical and non-binding.",
  "aligned_at": "2026-07-27T00:48:10.693464Z",
  "generic_source_depth": {
    "excerpts": true,
    "study": true,
    "adopted_at": "2026-07-27T01:30:27.859088+00:00",
    "upstream": "generic-swarm-ops/business/video/agents"
  }
}
```

### 11.4 Provenance narrative (SPEC)

- Pack agent ID `video.colorist` is aligned to pure VA Domain Pack / generic pack taxonomy (Agent IDs ≈ VA tables). - Previous common inventory ID `video.colorist` is historical mapping only. - Upstream design body below is **historical and non-binding**; local `agent_spec.json` is authoritative. - Full VA/generic SPEC depth retained for offline design fidelity (including category roster rows and common agent structure when present upstream).

### 11.5 VA roster row (historical design)

From `va-agent-swarm/study/agents.md` row **#10 Self-Quality: L3 Preference** (design corpus — non-binding for activation).

| Dimension | Content |
|-----------|---------|
| Responsibility | Would target audience choose this over human baseline? |
| Knowledge distillation | Pairwise comparison: AudienceSim panel (≥200 simulated personas + ≥20 HiTL samples). Win rate ≥50% (parity) or ≥55% (surpass). |
| Self-quality |  |
| Surpass-human | 11 |
| Accepts critique | **Surpass-Human Signal** |
| Comments on | Pre-registered proof the agent exceeds a credentialed professional |
| Tools (historical) | Benchmark dominance; blind Arena preference ≥55%; speed × quality (equal L2 at ≤10% turnaround); lower 90-day defect rate; certification pass; higher novelty at equal quality. |
| Architecture |  |

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/aesthetics_agent_functional_specification_hk.md` | **綜合功能規格：美學代理 — 為 VA-Agent-Swarm 提供運算「藝術家感知 (Artiste Sense)」的評審與對齊服務** **文件版本：** 1.0（最終版 - 完整且詳盡） **日期：** 2026 年 5 月 29 日 **作者：** Grok (xAI) + 與使用者 Nicholas (nicholas_hui) 的協同迭代 **目標受眾：** 高級人工智慧工程/編碼代理（立即實施） **目的：** 這是用於構建 **美學代理 (Aesthetics Agent)** 的 **最終生產級規範** — 一個有狀態的、多模態的評估與對齊服務，為整個 VA-Agent-Swarm 操作化一種模擬的 **「藝術家感知」**（藝術/美學直覺）。它是對 [`aesthetics_agents.md`](./aesthetics_agents.md) 的深度重新思考：該文件是一份 *方法綜述*，而本文件是一份 *可建構的代理契約*。它將「教 AI 藝術感知」從單一圖像評分器重新建構為一個 **共享的美學神經系統** — 一個每個生成代理都會諮詢的評審、一個每個微調迴圈都信任的獎勵訊號，以及一個將導演、品牌或藝術家的品味貫穿整條生產線的個人化引擎。 --- ### 1. (score 4) |

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json)
- [Folder README](README.md)
- [Provenance](sources/PROVENANCE.json)
- [Mapping note](sources/MAPPING.md)
- [Pack inventory](../../inventory.json)
- [Pack manifest](../../manifest.json)
- All required primary references resolve inside this repository.

### 12.4 Folder README (summary)

VA-aligned Domain Pack agent (common host). | File | Purpose | |------|----------| | `SPEC.md` | VA/generic depth + host binding | | `agent_spec.json` | Fail-closed runtime | | `sources/` | Provenance / mapping |

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "agent_id": "video.colorist",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.colorist.v1",
  "role": "ColoristAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.colorist.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 10,
  "va_name": "ColoristAgent",
  "va_category": "3-Edit"
}
```

<details><summary>SPEC runtime binding prose</summary>

Authoritative fail-closed host configuration:

```json
{
  "agent_id": "video.colorist",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.colorist.v1",
  "role": "ColoristAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.colorist.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 10,
  "va_name": "ColoristAgent",
  "va_category": "3-Edit"
}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Final grade; look consistency
Host role binding: `ColoristAgent (VA Domain Pack)`. Design-time VA table content below is historical and non-binding for activation.
### Responsibility (from VA table)
Final grade; look consistency
### Knowledge distillation sources (historical)
ICA corpora; Sonnenfeld sessions; HPA Award grades
### Self-quality criteria (historical)
ΔE drift <2; skin-tone IT8 alignment; mood vector match
### Surpass-human signal (historical)
Beats junior colorist in blind preference; matches senior within ΔE
### Critique bus (historical)
- **Accepts critique from:** DoPAgent, DirectorAgent, AccessibilityAgent (contrast)
- **Comments on:** DoPAgent (mixed-temp), VFXAgent (comp-color mismatch)
### Tools design-time notes (historical, non-activating)
DaVinci Resolve color API (MCP); ACES/OCIO pipeline; LUT generators
**Runtime safety:** Host allow-lists are only `agent_spec.json` + `tool-permission-register.json`. CI uses video_* stubs. Do not treat design-time vendor names as enabled APIs.
### Architecture pattern (historical)
Self-Refine + tool-use (colorimeter validation)

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/video/agents/video.colorist/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **ColoristAgent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
