# IdeationAgent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/video/agents/video.ideation/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**IdeationAgent** (`video.ideation`) is a Video Domain Pack role on CASOPS. In plain terms: Divergent brainstorm of concepts, hooks, taglines. Treat this guide as the human-readable operator map for catalog review, swarm design, and proposal-driven improvement — not as a runtime activation license.

| | |
|---|---|
| **Display name** | IdeationAgent |
| **Pack agent id** | `video.ideation` |
| **Pack** | `video` |
| **Host role string** | IdeationAgent (VA Domain Pack) |
| **Catalog status** | `registered` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `59` |
| **VA category** | `9-Meta` — Meta / optimization specialists |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `video.prompt.ideation.v1` / `video.rubric.ideation.v1` |
| **Previous common id** | `video.concept_developer` |
| **Generic lineage** | `generic-swarm-ops/business/video/agents/video.ideation` |

## 2. Why this role exists

Human video crews already solve this problem under time pressure: Divergent brainstorm of concepts, hooks, taglines. The agent form exists so the multi-agent system can **encode that craft continuously**, apply it on every handoff, and escalate only the residual risk to humans.

Its knowledge base is distilled from: **Cannes Grand Prix; D&AD; IDEO design-thinking; SCAMPER/de Bono** — historical design sources, not live network calls on this host.

The design ambition (surpass-human signal) is: *Wins agency-pitch shootouts on concept density.* Use that as the north-star metric when you write evals or proposals later.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Divergent brainstorm of concepts, hooks, taglines.
- **Self-quality bar:** Idea-count; novelty (embedding distance); semantic diversity
- **Ambition signal (design-time):** Wins agency-pitch shootouts on concept density
- **Architecture pattern (design-time):** Self-Refine + NoveltyAgent as critic

### Host quality & critique binding

- Prompt reference: `video.prompt.ideation.v1`
- Rubric reference: `video.rubric.ideation.v1`
- Critique edges: `{"inputs": ["video.critic"], "outputs": ["video.judge"]}`
- Max refinement: `3`
- VA table quality criteria retained under Provenance and Identity surface above.

### Inputs → outputs → acceptance

- Inputs: local pack artifacts and typed handoffs.
- Outputs: reviewable video-domain deliverables with acceptance criteria.
- Acceptance: host policy plus local SPEC criteria; no external repository required.

## 4. Scope: owns, shares, and refuses

### Owns

Divergent brainstorm of concepts, hooks, taglines.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** CreativeDirectorAgent, NoveltyAgent
- **Is qualified to comment on:** CopywriterAgent (derivative), DirectorAgent (unfilmable)
- **Host critique edge inputs:** `['video.critic']`
- **Host critique edge outputs:** `['video.judge']`
- **Max refinement count:** `3`

### Refuses / escalates

- Fail-closed: no provider activation, no network, no credentials from design text.
- `production_activation_requested` remains false unless a separate human gate changes it.
- Escalates legal, safety, rights, and release decisions to required human gates.
- Critique lead / judge defaults use VA IDs `video.critic` and `video.judge`.

## 5. When to involve this agent

- **Call this agent when** the work product needs a decision or artifact owned by *IdeationAgent*: Divergent brainstorm of concepts, hooks, taglines
- **Pipeline phase:** Meta / optimization specialists. Cross-cutting optimization, evaluation, and system improvement roles.
- **Do not call it for** generic orchestration, budget greenlight, or final legal sign-off unless those duties are explicitly in its responsibility — use producer/legal/compliance peers instead.
- **Human override:** any release-blocking safety, rights, or accessibility dispute escalates to a human gate even if the agent would auto-pass in a future active runtime.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *IdeationAgent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | CreativeDirectorAgent, NoveltyAgent |
| **Outbound commentary** | CopywriterAgent (derivative), DirectorAgent (unfilmable) |
| **Host edge inputs** | ['video.critic'] |
| **Host edge outputs** | ['video.judge'] |

## 7. Where it sits in production

### 7.1 Human production workflow

Cross-cutting optimization, evaluation, and system improvement roles.

**Typical handoff artifacts:** Eval reports, optimization tickets, benchmark deltas, harness results.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Observer and optimizer nodes over the full agent graph (latency, cost, prompts, retention, ROAS).

Design-time tool notes (non-binding on host): Embedding novelty scorer; concept clustering (UMAP); Are.na/Pinterest search

Loop style to emulate when you later implement runtime: **Self-Refine + NoveltyAgent as critic**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

**Mentions in local AI-workflow excerpt:**

- 59 — **IdeationAgent** — Divergent brainstorm of concepts, hooks, taglines, what-if angles — Cannes Lions Grand Prix archive; D&AD winners; IDEO design-thinking corpus; SCAMPER / Lateral Thinking (de Bono) — Idea-count per brief; novelty (embedding distance from corpus); semantic diversity within batch — Wins blind agency-pitch shootouts on first-round concept density — CreativeDirectorAgent, Nove
- 64 — **NoveltyAgent / Anti-Cliché Critic** — Flags tropes, clichés, and over-fit-to-corpus outputs — TV Tropes; OpenSubtitles n-gram frequency; corpus-novelty embeddings — Cliché-hit count per output; novelty score relative to category prior — Catches more clichés than experienced script editor in blind eval — IdeationAgent, ScreenwriterAgent — ScreenwriterAgent (trope-stuffed), CopywriterAgent (t
- 68 — **TrendIntelligenceAgent** — Detects emerging memes, sounds, formats with lead time — TikTok Creative Center, Trendpop, Tubular, Sensor Tower, Reddit/X firehose — Trend-prediction lead time vs viral peak; precision/recall on trend list — Earlier detection than social-strategist humans at higher precision — SocialStrategistAgent, CopywriterAgent — IdeationAgent (off-trend)

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/video.ideation` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/video.ideation/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `2048` / out `1024` / tool requests `0`.
- Design-time tool catalogue (non-activating): Embedding novelty scorer; concept clustering (UMAP); Are.na/Pinterest search

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `video.rubric.ideation.v1` against: Idea-count; novelty (embedding distance); semantic diversity
2. **Prompt precision** — align `video.prompt.ideation.v1` with responsibility: Divergent brainstorm of concepts, hooks, taglines.
3. **Critique graph** — ensure inbound `CreativeDirectorAgent, NoveltyAgent` and host edges `['video.critic']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Self-Refine + NoveltyAgent as critic
5. **Eval cases** — write fixed fixtures that would fail if *Wins agency-pitch shootouts on concept density* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Idea-count; novelty (embedding distance); semantic diversity |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |
| `sources/excerpts/agent_loop_v3.md` | `historical-excerpt` |
| `sources/excerpts/agents.md` | `historical-excerpt` |
| `sources/excerpts/ai_agent_video_production_workflow.md` | `historical-excerpt` |
| `sources/excerpts/complex_problem_solution_process_model.md` | `historical-excerpt` |
| `sources/excerpts/lifes_quiet_redemption_agent_workflow.md` | `historical-excerpt` |
| `sources/excerpts/system_build_plan.md` | `historical-excerpt` |
| `sources/excerpts/SYSTEM_REFERENCE.md` | `historical-excerpt` |
| `sources/excerpts/ui_design.md` | `historical-excerpt` |
| `sources/generic/video.conceptartist.SPEC.md` | `historical-generic` |
| `sources/generic/video.ideation.SPEC.md` | `historical-generic` |

### 11.2 Mapping note

# Mapping — `video.ideation` - VA/generic pack ID: `video.ideation` - Previous common ID: `video.concept_developer` - SPEC depth: full generic SPEC body + host runtime binding

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "agent_id": "video.ideation",
  "previous_common_agent_id": "video.concept_developer",
  "va_taxonomy_aligned": true,
  "generic_source": "generic-swarm-ops/business/video/agents/video.ideation",
  "note": "Agent ID aligned to VA/generic Domain Pack taxonomy. Upstream design is historical and non-binding.",
  "aligned_at": "2026-07-27T00:48:10.504343Z",
  "generic_source_depth": {
    "excerpts": true,
    "study": true,
    "adopted_at": "2026-07-27T01:30:28.225766+00:00",
    "upstream": "generic-swarm-ops/business/video/agents"
  }
}
```

### 11.4 Provenance narrative (SPEC)

- Pack agent ID `video.ideation` is aligned to pure VA Domain Pack / generic pack taxonomy (Agent IDs ≈ VA tables). - Previous common inventory ID `video.concept_developer` is historical mapping only. - Upstream design body below is **historical and non-binding**; local `agent_spec.json` is authoritative. - Full VA/generic SPEC depth retained for offline design fidelity (including category roster rows and common agent structure when present upstream).

### 11.5 VA roster row (historical design)

From `va-agent-swarm/study/agents.md` row **#59 IdeationAgent** (design corpus — non-binding for activation).

| Dimension | Content |
|-----------|---------|
| Responsibility | Divergent brainstorm of concepts, hooks, taglines |
| Knowledge distillation | Cannes Grand Prix; D&AD; IDEO design-thinking; SCAMPER/de Bono |
| Self-quality | Idea-count; novelty (embedding distance); semantic diversity |
| Surpass-human | Wins agency-pitch shootouts on concept density |
| Accepts critique | CreativeDirectorAgent, NoveltyAgent |
| Comments on | CopywriterAgent (derivative), DirectorAgent (unfilmable) |
| Tools (historical) | Embedding novelty scorer; concept clustering (UMAP); Are.na/Pinterest search |
| Architecture | Self-Refine + NoveltyAgent as critic |

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

_No strong keyword matches under va-agent-swarm; rely on local SPEC and VA table row._

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json)
- [Folder README](README.md)
- [Provenance](sources/PROVENANCE.json)
- [Mapping note](sources/MAPPING.md)
- [Pack inventory](../../inventory.json)
- [Pack manifest](../../manifest.json)
- All required primary references resolve inside this repository.

### 12.4 Folder README (summary)

VA-aligned Domain Pack agent (common host). | File | Purpose | |------|----------| | `SPEC.md` | VA/generic depth + host binding | | `agent_spec.json` | Fail-closed runtime | | `sources/` | Provenance / mapping |

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "agent_id": "video.ideation",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.ideation.v1",
  "role": "IdeationAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.ideation.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 59,
  "va_name": "IdeationAgent",
  "va_category": "9-Meta"
}
```

<details><summary>SPEC runtime binding prose</summary>

Authoritative fail-closed host configuration:

```json
{
  "agent_id": "video.ideation",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.ideation.v1",
  "role": "IdeationAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.ideation.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 59,
  "va_name": "IdeationAgent",
  "va_category": "9-Meta"
}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Divergent brainstorm of concepts, hooks, taglines
Host role binding: `IdeationAgent (VA Domain Pack)`. Design-time VA table content below is historical and non-binding for activation.
### Responsibility (from VA table)
Divergent brainstorm of concepts, hooks, taglines
### Knowledge distillation sources (historical)
Cannes Grand Prix; D&AD; IDEO design-thinking; SCAMPER/de Bono
### Self-quality criteria (historical)
Idea-count; novelty (embedding distance); semantic diversity
### Surpass-human signal (historical)
Wins agency-pitch shootouts on concept density
### Critique bus (historical)
- **Accepts critique from:** CreativeDirectorAgent, NoveltyAgent
- **Comments on:** CopywriterAgent (derivative), DirectorAgent (unfilmable)
### Tools design-time notes (historical, non-activating)
Embedding novelty scorer; concept clustering (UMAP); Are.na/Pinterest search
**Runtime safety:** Host allow-lists are only `agent_spec.json` + `tool-permission-register.json`. CI uses video_* stubs. Do not treat design-time vendor names as enabled APIs.
### Architecture pattern (historical)
Self-Refine + NoveltyAgent as critic

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/video/agents/video.ideation/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **IdeationAgent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
