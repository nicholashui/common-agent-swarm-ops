# Complex Problem Solution Process Model — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.complex-problem-solution-process-model/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Complex Problem Solution Process Model** (`specials.complex-problem-solution-process-model`) is a Specials pack capability on CASOPS. In plain terms: Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Complex Problem Solution Process Model |
| **Pack agent id** | `specials.complex-problem-solution-process-model` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.complex-problem-solution-process-model-prompt` / `spagent.complex-problem-solution-process-model-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
- **Self-quality bar:** Traceable outputs for complex problem-solving process; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent complex problem-solving process outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** Complex problem-solving process pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.complex-problem-solution-process-model-rubric` (inert identifier).
- Prompt reference: `spagent.complex-problem-solution-process-model-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.complex-problem-solution-process-model-input"],"outputs":["spagent.complex-problem-solution-process-model-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in complex problem-solving process, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.complex-problem-solution-process-model-input']`
- **Host critique edge outputs:** `['spagent.complex-problem-solution-process-model-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** Ill-structured problems need an explicit multi-stage solution process model.
- **Capability family:** Complex problem-solving process
- **Typical upstream:** Intent / research.
- **Typical downstream:** Planner, strategic goal, controller.
- **Concrete mission fragment:** Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Complex Problem Solution Process Model*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in complex problem-solving process, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.complex-problem-solution-process-model-input'] |
| **Host edge outputs** | ['spagent.complex-problem-solution-process-model-output'] |

### Specials lane placement

- **Family:** Complex problem-solving process
- **Upstream:** Intent / research.
- **Downstream:** Planner, strategic goal, controller.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **Complex problem-solving process pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.complex-problem-solution-process-model` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.complex-problem-solution-process-model/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.complex-problem-solution-process-model-rubric` against: Traceable outputs for complex problem-solving process; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.complex-problem-solution-process-model-prompt` with responsibility: Owns the specials-domain complex problem solution process model design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.complex-problem-solution-process-model-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Complex problem-solving process pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent complex problem-solving process outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for complex problem-solving process; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.complex-problem-solution-process-model` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/complex_problem_solution_process_model.md` - Source SHA-256: `89a4c10cd30206f2de051ba022946ebce8ef850d57f07c8af4d6cc592b7d014e` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.complex-problem-solution-process-model",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/complex_problem_solution_process_model.md"
  ],
  "source_sha256": "89a4c10cd30206f2de051ba022946ebce8ef850d57f07c8af4d6cc592b7d014e",
  "configuration_sha256": "fdfa09f46c0cc2d897a0da554ad75d2ed86f3172f349122120287558c8c518ca",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:45.925895Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/complex_problem_solution_process_model.md` - Design source SHA-256 (at generation): `89a4c10cd30206f2de051ba022946ebce8ef850d57f07c8af4d6cc592b7d014e` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/complex_problem_solution_process_model.md` | # Complex Problem Solution Process Model ## Introduction The Complex Problem Solution Process Model is a structured approach for solving complex, ill-defined, and non-immediate but important problems. (score 7) |
| `study/complex_problem_solution_process_model_hk.md` | # 複雜問題解決過程模型 ## Introduction 複雜問題解決過程模型是一種結構化方法，用於解決複雜、不明確、非直接但重要的問題。它專為以下情況而設計：正確答案不明顯、多種原因可能相互作用、直接跳到解決方案通常會導致浪費精力或解決錯誤的問題。該模型將問題解決組織為從理解問題到診斷其原因、產生和選擇解決方案、實施這些解決方案以及審查持續改進過程的分階段進展。 此模型的核心遵循五個相互連接的階段：`WHAT`、`WHY`、`HOW`、`DO` 和`REVIEW`。每個階段都有不同的目的。 `WHAT` 框架問題並定義工作的界限。 `WHY` 診斷根本原因。 `HOW` 開發並選擇替代解決方案。 `DO` 專注於執行、溝通、領導和專案管理。 `REVIEW` 確保流程保持自適應、自我糾正並適合未來使用。 該模型假設高品質的問題解決取決於仔細的框架、基於證據的推理、嚴格的假設檢驗、明確的決策標準和有效的利害關係人溝通。它也假設複雜的問題需要思考的廣度和深度。因此，理想的問題解決者是「T 形」的：足夠廣泛，可以跨學科連接想法，並且足夠深入，可以在相關領域進行嚴格推理。 ## 內容：提出問題 解決問題從框架開始。框架至關重要，因為定義問題的方式強烈影響如何理解問題以及如何解決問題。如果框架有缺陷，那麼剩下的努力可能會建立在一個薄弱的基礎上。 首要任務是明確問題所在。問題定義卡 (score 5) |

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/complex_problem_solution_process_model.md`

**Outline (for navigation):**

- Complex Problem Solution Process Model
- Introduction
- WHAT: Frame the Problem
- WHY: Diagnose the Problem
- HOW: Find and Select Solutions

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

#### Complex Problem Solution Process Model

#### Introduction
The Complex Problem Solution Process Model is a structured approach for solving complex, ill-defined, and non-immediate but important problems. It is designed for situations in which the right answer is not obvious, where multiple causes may interact, and where jumping directly to a solution often leads to wasted effort or solving the wrong problem. The model organizes problem solving as a staged progression from understanding the problem to diagnosing its causes, generating and selecting solutions, implementing those solutions, and reviewing the process for continuous improvement.

At its core, the model follows five connected stages: `WHAT`, `WHY`, `HOW`, `DO`, and `REVIEW`. Each stage has a distinct purpose. `WHAT` frames the problem and defines the boundaries of the effort. `WHY` diagnoses root causes. `HOW` develops and selects alternative solutions. `DO` focuses on execution, communication, leadership, and project management. `REVIEW` ensures that the process remains adaptive, self-correcting, and suitable for future use.

The model assumes that high-quality problem solving depends on careful framing, evidence-based reasoning, disciplined hypothesis testing, explicit decision criteria, and effective stakeholder communication. It also assumes that complex problems require both breadth and depth of thinking. The ideal problem solver is therefore "T-shaped": broad enough to connect ideas across disciplines, and deep enough to reason rigorously within relevant domains.

#### WHAT: Frame the Problem
Problem solving begins with framing. Framing is critical because the way a problem is defined strongly influences how it is understood and therefore how it will be solved. If the frame is flawed, the rest of the effort may be built on a weak foundation.

The first task is to identify the problem clearly. A problem definition card helps establish what the project is, what it is not, who the decision makers are, what resources are available, how much time is expected for each phase, and what risks must be managed. This card forces clarity and prevents teams from working with inconsistent assumptions.

Problem framing requires both divergent and convergent thinking. Divergent thinking helps generate multiple possible ways to describe the problem. Convergent thinking helps select the frame that is most useful. This stage also requires resisting conditioning. Past solutions may no longer fit present circumstances, and intuition alone is often unreliable when the stakes are high or the environment has changed.

Because complex problems are often ill-defined, the model emphasizes deliberate System 2 thinking over fast, intuitive judgment. Teams are encouraged to defer judgment, involve other people, keep probing, and rely on evidence rather than confidence. Members should continuously ask whether the current understanding reflects reality or whether it has been distorted by assumptions, habits, or overly narrow constraints.

Framing also includes defining scope. Teams must agree on project objectives, scope boundaries, and what success will look like. The scope should be neither too broad nor too narrow. A scope that is too broad makes the analysis unfocused; a scope that is too narrow may exclude the real source of the problem. New evidence discovered later may require changing the scope, and such changes should be reflected explicitly in the working definition of the problem.

An essential part of framing is forming the key diagnostic question. The model distinguishes between `what`, `why`, and `how` questions. In practice, the most important analytical questions are usually `why` and `how`. A `why` question is diagnostic: it seeks root causes. A `how` question is prescriptive: it seeks alternative solutions. The model warns against asking `how` before `why` has been understood. Good key questions are chosen for their type, topic, scope, and phrasing. Every word matters because imprecise language often signals weak logic.

To communicate the frame clearly, the model recommends an introductory flow based on `situation`, `complication`, and `question`. The situation defines the current reality using only necessary, sufficient, concise, and uncontested information. The complication introduces the disruption or tension that creates the need for change. The key question then becomes the logical destination of the situation-complication sequence. This structure helps audiences

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.complex-problem-solution-process-model.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.complex-problem-solution-process-model",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.complex-problem-solution-process-model-prompt",
  "rubric_reference": "spagent.complex-problem-solution-process-model-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.complex-problem-solution-process-model-input"
    ],
    "outputs": [
      "spagent.complex-problem-solution-process-model-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.complex-problem-solution-process-model","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.complex-problem-solution-process-model-prompt","rubric_reference":"spagent.complex-problem-solution-process-model-rubric","critique_edges":{"inputs":["spagent.complex-problem-solution-process-model-input"],"outputs":["spagent.complex-problem-solution-process-model-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain complex problem solution process model design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
At its core, the model follows five connected stages: `WHAT`, `WHY`, `HOW`, `DO`, and `REVIEW`. Each stage has a distinct purpose. `WHAT` frames the problem and defines the boundaries of the effort. `WHY` diagnoses root causes. `HOW` develops and selects alternative solutions. `DO` focuses on execution, communication, leadership, and project management. `REVIEW` ensures that the process remains adaptive, self-correcting, and suitable for future use. The model assumes that high-quality problem solving depends on careful framing, evidence-based reasoning, disciplined hypothesis testing, explicit decision criteria, and effective stakeholder communication. It also assumes that complex problems require both breadth and depth of thinking. The ideal problem solver is therefore "T-shaped": broad enough to connect ideas across disciplines, and deep enough to reason rigorously within relevant domains.

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.complex-problem-solution-process-model/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Complex Problem Solution Process Model**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
