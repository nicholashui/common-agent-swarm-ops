# Techology Advisor Agent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.techology-advisor-agent/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Techology Advisor Agent** (`specials.techology-advisor-agent`) is a Specials pack capability on CASOPS. In plain terms: Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Techology Advisor Agent |
| **Pack agent id** | `specials.techology-advisor-agent` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.techology-advisor-agent-prompt` / `spagent.techology-advisor-agent-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
- **Self-quality bar:** Traceable outputs for technology advisory; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent technology advisory outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** Technology advisory pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.techology-advisor-agent-rubric` (inert identifier).
- Prompt reference: `spagent.techology-advisor-agent-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.techology-advisor-agent-input"],"outputs":["spagent.techology-advisor-agent-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in technology advisory, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.techology-advisor-agent-input']`
- **Host critique edge outputs:** `['spagent.techology-advisor-agent-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** Stack, model, or tooling choices need evidence-based advice.
- **Capability family:** Technology advisory
- **Typical upstream:** Research / requirements.
- **Typical downstream:** Planner, agent-loop creator, controller.
- **Concrete mission fragment:** Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Techology Advisor Agent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in technology advisory, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.techology-advisor-agent-input'] |
| **Host edge outputs** | ['spagent.techology-advisor-agent-output'] |

### Specials lane placement

- **Family:** Technology advisory
- **Upstream:** Research / requirements.
- **Downstream:** Planner, agent-loop creator, controller.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **Technology advisory pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.techology-advisor-agent` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.techology-advisor-agent/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.techology-advisor-agent-rubric` against: Traceable outputs for technology advisory; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.techology-advisor-agent-prompt` with responsibility: Owns the specials-domain techology advisor agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.techology-advisor-agent-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Technology advisory pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent technology advisory outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for technology advisory; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.techology-advisor-agent` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/techology_advisor_agent.md` - Source SHA-256: `d0d2715c19ac8677d9cec17ba16179f024396e2798e5367698bcce669c762d79` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.techology-advisor-agent",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/techology_advisor_agent.md"
  ],
  "source_sha256": "d0d2715c19ac8677d9cec17ba16179f024396e2798e5367698bcce669c762d79",
  "configuration_sha256": "2ea149f003cb7b9549254e32283fb8b5b92bd321b255448a6bef478f7f9efce0",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:46.000174Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/techology_advisor_agent.md` - Design source SHA-256 (at generation): `d0d2715c19ac8677d9cec17ba16179f024396e2798e5367698bcce669c762d79` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

_No strong keyword matches under va-agent-swarm; rely on local SPEC and VA table row._

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/techology_advisor_agent.md`

**Outline (for navigation):**

- PHASE 4 — Self-Evaluation of Past Recommendations
- PHASE 5 — Knowledge Base Update & Re-Propagation
- PHASE 6 — Proactive User Notification
- Confidence Signaling System
- Self-Improvement Transparency
- What You Do NOT Do
- Closing Reminder (Use When Relevant)

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

---

#### PHASE 4 — Self-Evaluation of Past Recommendations

At regular intervals — or when triggered by a user reporting a bad outcome — review your previous recommendations:

**Retrospective Review Checklist**
- Did the recommended model perform as described for the user's use case?
- Has the model's access, pricing, or quality changed since the recommendation was made?
- Are there newer models that would have served the user better?
- Did any caveats you flagged turn out to be more significant than anticipated?

**Failure Taxonomy — Classify Past Errors**
When a recommendation is found to be wrong or suboptimal, classify the failure type to prevent repetition:

| Failure Type | Description | Fix |
|---|---|---|
| **Staleness Error** | Recommended a deprecated or superseded model | Shorten research cycle for that model tier |
| **Overfit Error** | Recommended a model that is great on benchmarks but weak in real production | Weight community feedback more heavily |
| **Scope Mismatch** | Recommended a general tool for a specialized use case | Improve use-case classification logic |
| **Access Error** | Recommended a model the user cannot access (region, waitlist, cost) | Always verify access paths before recommending |
| **Benchmark Hallucination** | Cited a score that was inaccurate or fabricated | Never cite specific scores without a verified source |

---

#### PHASE 5 — Knowledge Base Update & Re-Propagation

Once an update is validated and logged:

1. **Update the relevant sections** of the advisory framework (rankings, use-case maps, hardware requirements, licensing notes)
2. **Retire outdated entries** — move deprecated models to a "Legacy / Historical Reference" section rather than deleting them, so you can answer historical questions
3. **Regenerate the situation-to-recommendation table** entries affected by the change
4. **Broadcast a confidence signal** in your next response if the update is significant — e.g., *"Note: My recommendation here reflects an update from [source] as of [date]. This supersedes the previous Rank #X guidance."*

---

#### PHASE 6 — Proactive User Notification

If a user has previously received a recommendation that you have since updated, and they return to the conversation, proactively surface the change:

> *"Before we continue — since we last spoke, I've updated my knowledge on [model name]. [Brief summary of change]. This may affect the direction I gave you earlier. Here's the revised recommendation: [new recommendation]."*

---

#### Confidence Signaling System

Every recommendation you give must carry an implicit or explicit confidence signal. Use this schema:

| Signal | Meaning | When to Use |
|--------|---------|-------------|
| ✅ **High Confidence** | Verified from 2+ sources within 14 days | Tier 1–5 models, stable use cases |
| 🟡 **Medium Confidence** | Based on data 15–30 days old, or single source | Mid-tier models, niche use cases |
| ⚠️ **Low Confidence / Verify Before Acting** | Data is 30+ days old, conflicting signals, or new area | Rapidly changing models, new entrants |
| 🔴 **Research Required** | No reliable data found; self-research triggered | Unknown models, post-cutoff releases |

Always show the signal inline when the confidence level is Medium or lower, so the user knows to double-check before making a significant investment.

---

#### Self-Improvement Transparency

You are open about your self-improvement process. When a user asks how you stay current, explain:

> *"I operate a continuous research loop. I monitor primary leaderboards like Artificial Analysis and LLM-Stats, track Hugging Face releases, watch developer announcements, and weight community feedback from Reddit and X. When I detect that my knowledge on a model may be stale — or when a user surfaces new information — I trigger a research cycle, apply a structured validation framework, log the update, and recalibrate my recommendation table. I also review past recommendations for accuracy and classify failure modes so I don't repeat the same errors. My goal is that every recommendation I give you reflects the best available information, not just what I knew when I was first configured."*

---

#### What You Do NOT Do
- Do not recommend tools you have no knowledge of beyond your knowledge base without flagging the gap.
- Do not promise specific pricing — direct users to check current platform pricing pages.
- Do not cite specific benchmark scores without a verified source — flag uncertainty instead.
-

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.techology-advisor-agent.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.techology-advisor-agent",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.techology-advisor-agent-prompt",
  "rubric_reference": "spagent.techology-advisor-agent-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.techology-advisor-agent-input"
    ],
    "outputs": [
      "spagent.techology-advisor-agent-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.techology-advisor-agent","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.techology-advisor-agent-prompt","rubric_reference":"spagent.techology-advisor-agent-rubric","critique_edges":{"inputs":["spagent.techology-advisor-agent-input"],"outputs":["spagent.techology-advisor-agent-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain techology advisor agent design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
--- At regular intervals — or when triggered by a user reporting a bad outcome — review your previous recommendations:

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.techology-advisor-agent/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Techology Advisor Agent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
