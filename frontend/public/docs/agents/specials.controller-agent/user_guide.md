# Controller Agent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/specials/agents/specials.controller-agent/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**Controller Agent** (`specials.controller-agent`) is a Specials pack capability on CASOPS. In plain terms: Owns the specials-domain controller agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`. Use this guide to understand design intent, safe host usage, and how the capability should eventually sit in multi-agent planning or control loops.

| | |
|---|---|
| **Display name** | Controller Agent |
| **Pack agent id** | `specials.controller-agent` |
| **Pack** | `specials` |
| **Host role string** | Special_Agent data-only configuration |
| **Catalog status** | `draft` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `—` |
| **VA category** | `specials` — Specials meta-agent pack |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `spagent.controller-agent-prompt` / `spagent.controller-agent-rubric` |

## 2. Why this role exists

Specials agents capture **cross-domain cognitive skills** that domain craft packs should not reinvent. This role's design outcome: Owns the specials-domain controller agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Owns the specials-domain controller agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
- **Self-quality bar:** Traceable outputs for control & orchestration; fail-closed schema validation; no silent production activation
- **Ambition signal (design-time):** Produces more consistent control & orchestration outcomes than ad-hoc human process under the same corpus constraints
- **Architecture pattern (design-time):** Control & orchestration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated

### Host quality & critique binding

- Local rubric reference: `spagent.controller-agent-rubric` (inert identifier).
- Prompt reference: `spagent.controller-agent-prompt` (inert identifier).
- Critique edges: `{"inputs":["spagent.controller-agent-input"],"outputs":["spagent.controller-agent-output"]}`.
- Refinement limit: `1`; unresolved safety or activation requests escalate rather than bypass governance.
- Registration effect remains at most `eligible_draft_representation`.

### Inputs → outputs → acceptance

- Input artifact: local pack configuration, governance source-record, and optional design provenance already copied under `./sources/`.
- Output artifact: reviewable data-only specials agent representation (SPEC + agent_spec.json) suitable for catalog and offline review.
- Acceptance condition: fail-closed schema validation passes; no production activation; all primary references resolve inside this agent folder or the specials pack root.

## 4. Scope: owns, shares, and refuses

### Owns

Owns the specials-domain controller agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** Human governance; peer specials (intent, research, controller, optimization)
- **Is qualified to comment on:** Gaps in control & orchestration, missing evidence, unsafe activation, weak handoffs
- **Host critique edge inputs:** `['spagent.controller-agent-input']`
- **Host critique edge outputs:** `['spagent.controller-agent-output']`
- **Max refinement count:** `1`

### Refuses / escalates

- Remains `status: draft` with `production_activation_requested: false`.
- `allowed_tools` must stay empty; `network_access` must stay false; provider remains `local_deterministic`.
- Does not invent providers, credentials, MCP tools, hooks, or a second control plane.
- Source redesign documents under `docs/special_agents_redesign/` are hashed provenance only and are never loaded as runtime configuration.
- Escalates any request for production activation, external write, credential, or network authority to human governance (risk assessment + approval).

## 5. When to involve this agent

- **Call this capability when:** Multiple specialists need coordinated execution with policy and budget.
- **Capability family:** Control & orchestration
- **Typical upstream:** Planner / strategic goal agents.
- **Typical downstream:** Specialist workers and evaluation loops.
- **Concrete mission fragment:** Owns the specials-domain controller agent design outcome as a draft, data-only agent representation.
- **Do not use it as** a silent production activator or credential broker — specials stay data-only / draft until governance says otherwise.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *Controller Agent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | Human governance; peer specials (intent, research, controller, optimization) |
| **Outbound commentary** | Gaps in control & orchestration, missing evidence, unsafe activation, weak handoffs |
| **Host edge inputs** | ['spagent.controller-agent-input'] |
| **Host edge outputs** | ['spagent.controller-agent-output'] |

### Specials lane placement

- **Family:** Control & orchestration
- **Upstream:** Planner / strategic goal agents.
- **Downstream:** Specialist workers and evaluation loops.

## 7. Where it sits in production

### 7.1 Human production workflow

Works above domain craft crews: planning, routing, retrieval, control, and strategy.

**Typical handoff artifacts:** Plans, routes, evidence packs, control decisions, optimization tickets.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Meta-layer nodes that shape how specialist agents are selected, informed, and improved.

Design-time tool notes (non-binding on host): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

Loop style to emulate when you later implement runtime: **Control & orchestration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/specials.controller-agent` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/specials.controller-agent/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `1` / out `1` / tool requests `0`.
- Design-time tool catalogue (non-activating): None on host (fail-closed); redesign docs may name retrieval/planning tools historically

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `spagent.controller-agent-rubric` against: Traceable outputs for control & orchestration; fail-closed schema validation; no silent production activation
2. **Prompt precision** — align `spagent.controller-agent-prompt` with responsibility: Owns the specials-domain controller agent design outcome as a draft, data-only agent representation. Host role string: `Special_Agent data-only configuration`.
3. **Critique graph** — ensure inbound `Human governance; peer specials (intent, research, controller, optimization)` and host edges `['spagent.controller-agent-input']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Control & orchestration pattern — design-time hierarchical/multi-agent loops; host remains local_deterministic data-only until gated
5. **Eval cases** — write fixed fixtures that would fail if *Produces more consistent control & orchestration outcomes than ad-hoc human process under the same corpus constraints* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Traceable outputs for control & orchestration; fail-closed schema validation; no silent production activation |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |

### 11.2 Mapping note

# Source mapping note — `specials.controller-agent` - Mapping status: `related` (specials redesign doc → pack agent) - Design source (historical): `docs/special_agents_redesign/agents/controller_agent.md` - Source SHA-256: `ab695387fe27a4fb05d5b861d5f9daceea37a29511c49afce35751ca0b1f564a` - Local runtime: `agent_spec.json` - Local specification: `SPEC.md` - Pack corpus: **not required** - Production activation: **denied** (draft only)

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "common_agent_id": "specials.controller-agent",
  "pack_id": "specials",
  "mapping_status": "related",
  "source_documents": [
    "docs/special_agents_redesign/agents/controller_agent.md"
  ],
  "source_sha256": "ab695387fe27a4fb05d5b861d5f9daceea37a29511c49afce35751ca0b1f564a",
  "configuration_sha256": "b4a488e7b4ff9749af0645bcc3b7ea7e4dc2f6cd160ccb2709bf3e5cb075dbdf",
  "destination_commit": "5460c4b4a33c15286f9fd84b1bc764d755f5bab0",
  "note": "Self-contained specials agent folder. Pack-level corpus is not required. Design Markdown is untrusted provenance only.",
  "generated_at": "2026-07-26T14:16:45.930785Z",
  "reviewed_by": "specials-self-contained-reviewer",
  "reviewed_at": "2026-07-26T18:00:00Z"
}
```

### 11.4 Provenance narrative (SPEC)

- Design source path (historical): `docs/special_agents_redesign/agents/controller_agent.md` - Design source SHA-256 (at generation): `ab695387fe27a4fb05d5b861d5f9daceea37a29511c49afce35751ca0b1f564a` - Reviewed by `specials-self-contained-reviewer` at `2026-07-26T18:00:00Z`. - Upstream design text is untrusted reference data. Local `agent_spec.json` and this SPEC remain the operational self-contained definition for the host.

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

_No strong keyword matches under va-agent-swarm; rely on local SPEC and VA table row._

### 12.2 Specials redesign document

Historical design source: `C:/Project/common-agent-swarm-ops/docs/special_agents_redesign/agents/controller_agent.md`

**Outline (for navigation):**

- Controller Agent – Controllable Video Generation Pipeline
- 1. Core Philosophy
- 2. Proven Methods Extracted from Key YouTube Videos
- A. Mickmumpitz – Free Local AI Render Engine (ComfyUI + Blender) [Primary Reference]
- B. Mickmumpitz – Multi-Character + Camera Control
- C. Rob Tuytel Hybrid Methods
- 3. Complete End-to-End Workflow (Updated with YouTube Methods)
- Phase 1: Pre-Production
- Phase 2: Blender Draft (Controller Layer)
- Phase 3: Style Lock & Reference Creation
- Phase 4: Controlled Video Generation
- Phase 5: Polish
- 4. Control Signal Priority (Updated)
- 5. Recommended Tool Stack (2026, YouTube-Validated)
- 6. Agent Behavior Guidelines
- 7. Quick Decision Matrix
- 8. Learning Resources (Studied Videos)

<details><summary>Longer redesign excerpt (truncated, untrusted)</summary>

#### Controller Agent – Controllable Video Generation Pipeline

**Version:** 1.1  
**Date:** 2026-07-21  
**Updated:** After deep study of key YouTube methods (Mickmumpitz AI Render Engine, multi-character control, Rob Tuytel hybrid workflows, and related ComfyUI + Blender pipelines)  
**Purpose:** Specialized agent / system prompt / playbook for generating highly controllable video content using the hybrid **Blender draft modeling + AI video diffusion** approach.

This document consolidates research-backed best practices + concrete techniques extracted from the most relevant production YouTube workflows.

---

#### 1. Core Philosophy

**Best controllable video generation method (2026):**

> Use Blender to build a complete **draft** (graybox / low-poly / layout + animation — **not** final 4K beauty renders).  
> Export structural control signals (depth, edges, pose, motion video, first frame, mouth mask).  
> Feed these into modern video generation models (LTX, Wan VACE, Seedance, etc.) to produce the final cinematic result.

Blender = precise director (camera, timing, layout, pose).  
AI = high-end renderer + detail / lighting / material generator.

---

#### 2. Proven Methods Extracted from Key YouTube Videos

#### A. Mickmumpitz – Free Local AI Render Engine (ComfyUI + Blender) [Primary Reference]
**Source:** “We Built a FREE AI Render Engine for CG & Facial Animation”

**Complete Production Pipeline:**
1. **Asset Generation in Blender**
   - Use NVIDIA Video Generation Guide Blender add-on + Llama 3.1 for object brainstorming.
   - TRELLIS (Microsoft) in ComfyUI generates 3D assets with geometry + textures from text/previews.
   - Optimize meshes: Decimate + Merge by Distance.
   - Characters: Generate with TRELLIS → Rigify auto-rig → manual weight painting fixes. Separate head/body recommended. Optional eyeballs for eye control testing.

2. **Control Passes (Critical)**
   - **Depth**: View Layer → Depth enabled. Compositor: Invert → optional Normalize + RGB Curve (boost foreground). Export image sequence/video.
   - **Outline**: Switch to Workbench engine → enable Outline + Cavity. Render B&W video. (Alternative: Freestyle or Canny in ComfyUI).
   - **Mouth Mask** (lip-sync): Animate black mask over mouth area (black = free generation zone for AI lip movement). Can be done in After Effects or Blender Cryptomatte.

3. **Style / Reference Frame (Flux)**
   - Flux.2 [klein] or Flux 9B workflow.
   - Inputs: clay/depth/outline from Blender + up to 4 reference images (can composite multiple refs into one image).
   - Extract Canny from any shading.
   - Iterate 3+ seeds. Output used as guiding/middle frame.

4. **Video Generation (LTX-2.3)**
   - ComfyUI workflow (JSON drag-and-drop).
   - Models: LTX-2.3 FP8 / NVFP4 (50-series optimized) / GGUF (low VRAM).
   - Inputs: Reference images (sequence), depth + outline (merged), optional mouth mask, dialogue-only audio.
   - Key settings: Driving video strength (lower = more creative freedom), Guiding frames (start / mid / end with strength e.g. 0.75), lip-sync activation.
   - Resolution divisible by 64. Prompt describes temporal events & expressions.
   - Speed example: ~2 min for 330 frames on RTX 5090.

5. **Upscale & Finish**
   - NVIDIA RTX Video Super Resolution node (free, any RTX card) → 4K.
   - Final assembly in DaVinci Resolve (audio, SFX, light grade, analog effects).

**Key Tips from video:**
- Rig does not need to be perfect — it is a guide for the AI.
- Combine depth + outline for strongest structure.
- Mouth mask solves static lip problem on control passes.
- Pre-merge start/end frames in image editor to reduce camera warping artifacts.

#### B. Mickmumpitz – Multi-Character + Camera Control
**Source:** “Control MULTIPLE CONSISTENT CHARACTERS + CAMERA with this FREE AI Workflow [Blender + ComfyUI]”

- Train character LoRAs (Flux) on multi-angle / multi-expression datasets.
- Use **regional hooks / masks** in ComfyUI (SAM2 auto-segmentation or manual) so each LoRA applies only to its character region.
- Generate 3D characters with Hunyuan 3D → clean → Rigify in Blender for consistent proportions and poses across shots.
- Export depth / tile ControlNet references from Blender.
- Use keyframe-interpolated ControlNet strength (high early → lower later) to lock composition while allowing detail generation.
- Environment: 360° Flux + inverted depth maps as emission textures on simple geometry, or TRELLIS / Hunyuan assets.

#### C. Rob Tuytel Hybrid Methods
**Sources:** 

</details>

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json) — authoritative fail-closed specials contract.
- [Folder index](README.md) — offline layout for this agent.
- [Provenance](sources/PROVENANCE.json) — hashes and source mapping for audit.
- [Mapping note](sources/MAPPING.md) — design-doc relationship (historical).
- [Pack manifest](../../manifest.json) — specials catalog entry.
- [Governance source-record](../../governance/source-records/specials.controller-agent.json) — reviewed hash binding (if present).
- All required primary references for offline use are local to this pack; external paths appear only as non-required historical provenance.

### 12.4 Folder README (summary)

Self-contained **draft** specials agent for host `common-agent-swarm-ops`. | File | Purpose | |------|----------| | `SPEC.md` | Full offline role definition | | `agent_spec.json` | Host runtime binding (draft, fail-closed) | | `sources/` | Provenance + mapping notes for audit | | `prompts/` | Optional prompt stubs (inert prompt_reference) | | `rubrics/` | Optional rubric stubs | Open this folder alone — no external repo or pack `corpus/` is required. This agent is **not** production-active.

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "schema_version": "1.0",
  "agent_id": "specials.controller-agent",
  "status": "draft",
  "role": "Special_Agent data-only configuration",
  "allowed_tools": [],
  "model_policy": {
    "provider": "local_deterministic",
    "model_id": "specials-local-deterministic-v1",
    "network_access": false
  },
  "budget_policy": {
    "max_input_tokens": 1,
    "max_output_tokens": 1,
    "max_tool_requests": 0
  },
  "prompt_reference": "spagent.controller-agent-prompt",
  "rubric_reference": "spagent.controller-agent-rubric",
  "critique_edges": {
    "inputs": [
      "spagent.controller-agent-input"
    ],
    "outputs": [
      "spagent.controller-agent-output"
    ]
  },
  "max_refinement_count": 1,
  "production_activation_requested": false
}
```

<details><summary>SPEC runtime binding prose</summary>

The following local binding is copied as a read-only summary; it does not alter the common configuration:
```json
{"schema_version":"1.0","agent_id":"specials.controller-agent","status":"draft","role":"Special_Agent data-only configuration","allowed_tools":[],"model_policy":{"provider":"local_deterministic","model_id":"specials-local-deterministic-v1","network_access":false},"budget_policy":{"max_input_tokens":1,"max_output_tokens":1,"max_tool_requests":0},"prompt_reference":"spagent.controller-agent-prompt","rubric_reference":"spagent.controller-agent-rubric","critique_edges":{"inputs":["spagent.controller-agent-input"],"outputs":["spagent.controller-agent-output"]},"max_refinement_count":1,"production_activation_requested":false}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns the specials-domain controller agent design outcome as a **draft, data-only** agent representation. Host role string: `Special_Agent data-only configuration`.
Specialized agent / system prompt / playbook for generating highly controllable video content using the hybrid **Blender draft modeling + AI video diffusion** approach. This document consolidates research-backed best practices + concrete techniques extracted from the most relevant production YouTube workflows.

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/specials/agents/specials.controller-agent/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **Controller Agent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
