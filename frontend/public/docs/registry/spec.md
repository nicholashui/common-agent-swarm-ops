# Registry — specification

## Route

`/registry`

## Behaviors

1. Local pack catalog projection (non-authority).
2. Filters are presentation-only.
3. Mutations require host action references.

> Missing docs for nested agent routes fall back to this folder after param strip.
