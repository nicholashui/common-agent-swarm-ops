# Video Agent Configuration

`manifest.json` is the authoritative non-active 114-agent configuration and `inventory.json` supplies its one-to-one inventory entries. These records are configuration data only; they do not create an execution loop, invoke a media tool, activate production work, or expose an API route.
