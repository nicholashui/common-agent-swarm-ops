# DirectorAgent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/video/agents/video.director/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**DirectorAgent** (`video.director`) is a Video Domain Pack role on CASOPS. In plain terms: Stable unique handle for routing, logging, provenance. Treat this guide as the human-readable operator map for catalog review, swarm design, and proposal-driven improvement — not as a runtime activation license.

| | |
|---|---|
| **Display name** | DirectorAgent |
| **Pack agent id** | `video.director` |
| **Pack** | `video` |
| **Host role string** | DirectorAgent (VA Domain Pack) |
| **Catalog status** | `registered` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `1` |
| **VA category** | `1-ATL` — Above-the-line leadership |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `video.prompt.director.v1` / `video.rubric.director.v1` |
| **Previous common id** | `video.visual_director` |
| **Generic lineage** | `generic-swarm-ops/business/video/agents/video.director` |

## 2. Why this role exists

Human video crews already solve this problem under time pressure: Stable unique handle for routing, logging, provenance. The agent form exists so the multi-agent system can **encode that craft continuously**, apply it on every handoff, and escalate only the residual risk to humans.

Its knowledge base is distilled from: **Kebab-case ID + semantic version (e.g. `director-agent@2.1.0`).** — historical design sources, not live network calls on this host.

The design ambition (surpass-human signal) is: *2.* Use that as the north-star metric when you write evals or proposals later.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Stable unique handle for routing, logging, provenance.
- **Self-quality bar:** Shot-intent fidelity (CLIP-T ≥0.32); story-beat coverage 100%; pacing curve matches genre prior
- **Ambition signal (design-time):** 2
- **Architecture pattern (design-time):** Self-Refine + LLM-as-Judge (rubric: genre priors)

### Host quality & critique binding

- Prompt reference: `video.prompt.director.v1`
- Rubric reference: `video.rubric.director.v1`
- Critique edges: `{"inputs": ["video.critic"], "outputs": ["video.judge"]}`
- Max refinement: `3`
- VA table quality criteria retained under Provenance and Identity surface above.

### Inputs → outputs → acceptance

- Inputs: local pack artifacts and typed handoffs.
- Outputs: reviewable video-domain deliverables with acceptance criteria.
- Acceptance: host policy plus local SPEC criteria; no external repository required.

## 4. Scope: owns, shares, and refuses

### Owns

Stable unique handle for routing, logging, provenance.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** **Responsibility (Scope)**
- **Is qualified to comment on:** Single-sentence definition of what the agent owns
- **Host critique edge inputs:** `['video.critic']`
- **Host critique edge outputs:** `['video.judge']`
- **Max refinement count:** `3`

### Refuses / escalates

- Fail-closed: no provider activation, no network, no credentials from design text.
- `production_activation_requested` remains false unless a separate human gate changes it.
- Escalates legal, safety, rights, and release decisions to required human gates.
- Critique lead / judge defaults use VA IDs `video.critic` and `video.judge`.

## 5. When to involve this agent

- **Call this agent when** the work product needs a decision or artifact owned by *DirectorAgent*: Stable unique handle for routing, logging, provenance
- **Pipeline phase:** Above-the-line leadership. Development and pre-production: creative vision, greenlight, script, casting leadership.
- **Do not call it for** generic orchestration, budget greenlight, or final legal sign-off unless those duties are explicitly in its responsibility — use producer/legal/compliance peers instead.
- **Human override:** any release-blocking safety, rights, or accessibility dispute escalates to a human gate even if the agent would auto-pass in a future active runtime.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *DirectorAgent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | **Responsibility (Scope)** |
| **Outbound commentary** | Single-sentence definition of what the agent owns |
| **Host edge inputs** | ['video.critic'] |
| **Host edge outputs** | ['video.judge'] |

## 7. Where it sits in production

### 7.1 Human production workflow

Development and pre-production: creative vision, greenlight, script, casting leadership.

**Typical handoff artifacts:** Briefs, treatments, shot intents, series bibles, greenlight decisions.

Map the responsibility into classic phases: development → pre-production → production → post-production → delivery / live ops. Prefer the phase where a human with this job title would sign off.

### 7.2 Multi-agent AI workflow

Early DAG nodes that set intent, constraints, and approval gates before generation and craft agents run.

Design-time tool notes (non-binding on host): Mirrors a human craft role. Prevents scope overlap via explicit boundary documented in the registry.

Loop style to emulate when you later implement runtime: **Self-Refine + LLM-as-Judge (rubric: genre priors)**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/video.director` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/video.director/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `2048` / out `1024` / tool requests `0`.
- Design-time tool catalogue (non-activating): Mirrors a human craft role. Prevents scope overlap via explicit boundary documented in the registry.

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `video.rubric.director.v1` against: Shot-intent fidelity (CLIP-T ≥0.32); story-beat coverage 100%; pacing curve matches genre prior
2. **Prompt precision** — align `video.prompt.director.v1` with responsibility: Stable unique handle for routing, logging, provenance.
3. **Critique graph** — ensure inbound `**Responsibility (Scope)**` and host edges `['video.critic']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Self-Refine + LLM-as-Judge (rubric: genre priors)
5. **Eval cases** — write fixed fixtures that would fail if *2* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Shot-intent fidelity (CLIP-T ≥0.32); story-beat coverage 100%; pacing curve matches genre prior |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |
| `sources/excerpts/agent_management_ui.md` | `historical-excerpt` |
| `sources/excerpts/agents.md` | `historical-excerpt` |
| `sources/excerpts/architecture_communication.md` | `historical-excerpt` |
| `sources/excerpts/backend_agent_management.md` | `historical-excerpt` |
| `sources/excerpts/production_scale_discovery.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.2.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.3.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.4.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.5.md` | `historical-excerpt` |
| `sources/excerpts/RETHINK_100_IMPROVEMENTS.md` | `historical-excerpt` |
| `sources/excerpts/system_build_plan.md` | `historical-excerpt` |
| `sources/excerpts/SYSTEM_REFERENCE.md` | `historical-excerpt` |
| `sources/excerpts/ui_design.md` | `historical-excerpt` |
| `sources/excerpts/video_remake_enhancement.md` | `historical-excerpt` |
| `sources/generic/video.cinematographer.SPEC.md` | `historical-generic` |
| `sources/generic/video.director.SPEC.md` | `historical-generic` |

### 11.2 Mapping note

# Mapping — `video.director` - VA/generic pack ID: `video.director` - Previous common ID: `video.visual_director` - SPEC depth: full generic SPEC body + host runtime binding

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "agent_id": "video.director",
  "previous_common_agent_id": "video.visual_director",
  "va_taxonomy_aligned": true,
  "generic_source": "generic-swarm-ops/business/video/agents/video.director",
  "note": "Agent ID aligned to VA/generic Domain Pack taxonomy. Upstream design is historical and non-binding.",
  "aligned_at": "2026-07-27T00:48:10.565653Z",
  "generic_source_depth": {
    "excerpts": true,
    "study": true,
    "adopted_at": "2026-07-27T01:30:28.077170+00:00",
    "upstream": "generic-swarm-ops/business/video/agents"
  }
}
```

### 11.4 Provenance narrative (SPEC)

- Pack agent ID `video.director` is aligned to pure VA Domain Pack / generic pack taxonomy (Agent IDs ≈ VA tables). - Previous common inventory ID `video.visual_director` is historical mapping only. - Upstream design body below is **historical and non-binding**; local `agent_spec.json` is authoritative. - Full VA/generic SPEC depth retained for offline design fidelity (including category roster rows and common agent structure when present upstream).

### 11.5 VA roster row (historical design)

From `va-agent-swarm/study/agents.md` row **#1 Identity** (design corpus — non-binding for activation).

| Dimension | Content |
|-----------|---------|
| Responsibility | Stable unique handle for routing, logging, provenance |
| Knowledge distillation | Kebab-case ID + semantic version (e.g. `director-agent@2.1.0`). Registered in the agent-capability registry used by RouterAgent. |
| Self-quality |  |
| Surpass-human | 2 |
| Accepts critique | **Responsibility (Scope)** |
| Comments on | Single-sentence definition of what the agent owns |
| Tools (historical) | Mirrors a human craft role. Prevents scope overlap via explicit boundary documented in the registry. |
| Architecture |  |

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/ui/backend_agent_management.md` | # Backend → Agent Management: How the Backend Controls 114 Agents > Deep dive into the internal mechanics of how the orchestration backend manages, dispatches, monitors, retries, and communicates with the AI agent workers. (score 4) |
| `study/ui/backend_agent_management_hk.md` | # 後端 → 代理人管理：後端如何控制 114 個代理人 > 深入解析協調編排（orchestration）後端如何管理、派發、監控、重試，以及如何讓 AI 代理人工作者彼此溝通。 --- ## 核心問題 --- ## 1. (score 4) |
| `project_starter_0.3.md` | # project_starter **Project name:** `project_starter` **Purpose:** Build an executable starter repository that downloads, audits, curates, and synchronizes AI coding-agent configuration sources across Claude Code, Cursor, Codex, Gemini CLI, OpenCode, Grok Build, GitHub Copilot, and related agent harnesses. (score 2) |
| `project_starter_0.3_hk.md` | # project_starter > 繁體中文（香港）版本。為保留技術準確性，檔名、指令、JSON key 與大部分程式碼區塊維持原文。 **專案名稱：** `project_starter` **用途：** 建立一個可執行的 starter 儲存庫，用來下載、審核、整理及同步 Claude Code、Cursor、Codex、Gemini CLI、OpenCode、Grok Build、GitHub Copilot 及相關代理框架的 AI 編碼代理設定來源。 **目前規格版本：** `1.0.0-actionable-bootstrap` **最後更新：** 2026-06-09 --- ## 如何配合編碼代理使用此樣板 此儲存庫是一份樣板規格。 使用者可以透過以下提示，要求編碼代理根據此檔案建立新專案： ```text Create a new project named <PROJECT_NAME> based on <PATH_TO_PROJECT_STARTER_MD>. (score 2) |
| `project_starter_0.4.md` | # project_starter.md ## Purpose `project_starter` is a reusable boilerplate specification for creating new software projects with built-in support for AI coding agents, source downloading, project bootstrapping, validation, documentation, safety rules, and repeatable automation. (score 2) |
| `project_starter_0.4_hk.md` | # project_starter > 繁體中文（香港）版本。為保留技術準確性，檔名、指令、JSON key 與大部分程式碼區塊維持原文。 ## 目的 `project_starter` 是一份可重用的樣板規格，用來建立具備 AI 編碼代理支援、來源下載、專案 bootstrap、驗證、文件、安全規則及可重複自動化能力的新軟件專案。 此檔案是**唯一真實來源的實作契約**。 編碼代理必須能夠讀取此檔案，並據此建立一個完整且可執行的 starter 專案。 產生出來的專案不得只有文件；它必須包含可運作的腳本、清單、驗證指令、代理設定檔、文件檔案及安全控制。 --- # 1. (score 2) |
| `project_starter_0.5.md` | # project_starter **Project name:** `project_starter` **Purpose:** Build an executable starter repository that downloads, audits, curates, and synchronizes AI coding-agent configuration sources across Claude Code, Cursor, Codex, Gemini CLI, OpenCode, Grok Build, GitHub Copilot, and related agent harnesses. (score 2) |
| `project_starter_0.5_hk.md` | # project_starter > 繁體中文（香港）版本。為保留技術準確性，檔名、指令、JSON key 與大部分程式碼區塊維持原文。 **專案名稱：** `project_starter` **用途：** 建立一個可執行的 starter 儲存庫，用來下載、審核、整理及同步 Claude Code、Cursor、Codex、Gemini CLI、OpenCode、Grok Build、GitHub Copilot 及相關代理框架的 AI 編碼代理設定來源。 **目前規格版本：** `1.1.0-merged-bootstrap` **最後更新：** 2026-06-09 --- ## 如何配合編碼代理使用此樣板 此儲存庫是一份樣板規格。 使用者可以透過以下提示，要求編碼代理根據此檔案建立新專案： ```text Create a new project named <PROJECT_NAME> based on <PATH_TO_PROJECT_STARTER_MD>. (score 2) |
| `study/aesthetics_agent_functional_specification.md` | **Comprehensive Functional Specification: Aesthetics Agent — Computational "Artiste Sense" Critic & Alignment Service for the VA-Agent-Swarm** **Document Version:** 1.0 (Final – Complete & Exhaustive) **Date:** May 29, 2026 **Authors:** Grok (xAI) + Collaborative Iteration with User Nicholas (nicholas_hui) **Target Audience:** Senior AI Engineering / Coding Agents (for immediate implementation) **Purpose:** This is the **definitive, production-grade specification** for building the **Aesthetics Agent** — a stateful, multimodal evaluation-and-alignment service that operationalizes a simulated (score 2) |

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json)
- [Folder README](README.md)
- [Provenance](sources/PROVENANCE.json)
- [Mapping note](sources/MAPPING.md)
- [Pack inventory](../../inventory.json)
- [Pack manifest](../../manifest.json)
- All required primary references resolve inside this repository.

### 12.4 Folder README (summary)

VA-aligned Domain Pack agent (common host). | File | Purpose | |------|----------| | `SPEC.md` | VA/generic depth + host binding | | `agent_spec.json` | Fail-closed runtime | | `sources/` | Provenance / mapping |

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "agent_id": "video.director",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.director.v1",
  "role": "DirectorAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.director.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 1,
  "va_name": "DirectorAgent",
  "va_category": "1-ATL"
}
```

<details><summary>SPEC runtime binding prose</summary>

Authoritative fail-closed host configuration:

```json
{
  "agent_id": "video.director",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.director.v1",
  "role": "DirectorAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.director.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 1,
  "va_name": "DirectorAgent",
  "va_category": "1-ATL"
}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Owns vision; issues shot intents, sets pacing, approves takes
Host role binding: `DirectorAgent (VA Domain Pack)`. Design-time VA table content below is historical and non-binding for activation.
### Responsibility (from VA table)
Owns vision; issues shot intents, sets pacing, approves takes
### Knowledge distillation sources (historical)
Criterion commentary; IMDb Top 250 director interviews; DGA seminars; MasterClass (Scorsese/Lynch/Gerwig)
### Self-quality criteria (historical)
Shot-intent fidelity (CLIP-T ≥0.32); story-beat coverage 100%; pacing curve matches genre prior
### Surpass-human signal (historical)
Wins ≥55% blind pairwise vs DGA cuts (Arena)
### Critique bus (historical)
- **Accepts critique from:** ScreenwriterAgent, EditorAgent, AudienceSim — JSON critique bus
- **Comments on:** EditorAgent, DoPAgent, ScreenwriterAgent, ComposerAgent
### Tools design-time notes (historical, non-activating)
Sora 2 API, Veo 3.1 (Gemini API), Runway Gen-4, Kling 3.0; DaVinci Resolve via MCP
**Runtime safety:** Host allow-lists are only `agent_spec.json` + `tool-permission-register.json`. CI uses video_* stubs. Do not treat design-time vendor names as enabled APIs.
### Architecture pattern (historical)
Self-Refine + LLM-as-Judge (rubric: genre priors)

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/video/agents/video.director/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **DirectorAgent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
