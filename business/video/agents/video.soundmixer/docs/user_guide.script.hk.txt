大家好，歡迎收睇今日呢條影片。

今日我哋會詳細介紹一個叫做 SoundMixerAgent 嘅角色，佢嘅全名係 Re-recording。

呢個係一個操作員同設計指南，專門俾操作員、pack 作者、swarm 設計師同埋審核人員睇嘅。

呢份指南會俾到你任務清晰度、幾時用嘅指引、協作地圖、CASOPS 操作步驟、改進路徑，同埋一個分信任層級嘅來源地圖。

好重要嘅一點係，呢份文件唔係一個去啟動 provider 嘅許可證。

亦唔係去開放網絡存取或者發明工具嘅授權。

Host 嘅真相係 fail-closed 嘅 agent_spec.json。

來自 VA 或者 redesign 文件嘅設計文字係唔受信任嘅來源。

呢個 pack 嘅路徑係 business/video/agents/video.soundmixer/。

指南位於 docs/user_guide.md。

呢份指南係由 scripts/business/generate_agent_user_guides.py 自動生成嘅。

第一部分，快照。

SoundMixerAgent，即 Re-recording，佢嘅 pack agent id 係 video.soundmixer。

佢係一個 Video Domain Pack 角色，運行喺 CASOPS 上面。

簡單嚟講，佢嘅工作就係做最終混音，同埋交付 5.1 聲道同 Atmos 格式嘅成品。

你要將呢份指南當作人類可讀嘅操作員地圖，用嚟做目錄審查、swarm 設計同埋透過提案推動改進，而唔係當作運行時啟動嘅許可證。

而家我逐項講解佢嘅基本資料。

Display name 係 SoundMixerAgent (Re-recording)。

Pack agent id 係 video.soundmixer。

Pack 係 video。

Host role string 係 SoundMixerAgent (Re-recording) (VA Domain Pack)。

Catalog status 係 registered。

Maturity today 係 L0，即非活動狀態，只有目錄同設計保真度。

VA id 係 22。

VA category 係 4-Snd，即聲音同音樂類別。

Provider，即 host，係 local_deterministic。

Network 係 False，即係冇網絡存取。

Allowed tools 係空嘅，即係冇任何被允許嘅工具。

Production activation requested 係 False。

Prompt 同 rubric 嘅參考分別係 video.prompt.soundmixer.v1 同 video.rubric.soundmixer.v1。

Previous common id 係 video.dialog_editor。

Generic lineage 係 generic-swarm-ops/business/video/agents/video.soundmixer。

第二部分，點解呢個角色存在。

人類嘅影片製作團隊已經喺時間壓力之下解決呢個問題，就係做最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Agent 形式嘅存在係為咗令多代理系統可以持續編碼呢種工藝。

佢會喺每次交接時都應用呢種工藝，並且只將殘餘風險升級俾人類處理。

佢嘅知識庫係從以下來源蒸餾而來嘅：CAS Awards，即 Cinema Audio Society 嘅獎項。

仲有 Atmos 規格同埋廣播響度標準。

呢啲係歷史設計來源，唔係喺呢個 host 上面做嘅即時網絡呼叫。

設計野心，即超越人類嘅信號，係：第一次就達到 CAS 規格而唔需要返工。

當你之後寫評估或者提案嘅時候，用呢個作為北極星指標。

第三部分，乜嘢叫做好。

呢度嘅標準係你喺閱讀輸出、撰寫評分標準或者評分提案時用嘅審查準則。

Primary outcome，即主要成果，係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Self-quality bar，即自我質量標準，包括三個方面。

第一個係 LUFS 目標。

第二個係 STOI 大於或等於 0.85。

第三個係規格交付物通過。

Ambition signal，即設計時嘅野心信號，係第一次就達到 CAS 規格而唔需要工程師返工。

Architecture pattern，即設計時嘅架構模式，係 Constitutional AI，憲法係廣播規格規則。

關於 Host 質量同批評綁定。

Prompt reference 係 video.prompt.soundmixer.v1。

Rubric reference 係 video.rubric.soundmixer.v1。

Critique edges 嘅 inputs 係 video.critic，outputs 係 video.judge。

Max refinement 係 3 次。

關於輸入、輸出同驗收。

Inputs 係本地 pack 工件同類型化嘅交接。

Outputs 係可審查嘅影片領域交付物，附帶驗收標準。

Acceptance 係 host 政策加上本地 SPEC 標準，唔需要外部存儲庫。

第四部分，範圍：擁有、共享同拒絕。

呢個 agent 擁有嘅範圍係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

佢同同事共享嘅批評表面方面，佢聽取同接受來自 EditorAgent 嘅批評。

佢亦接受來自 SoundDesignAgent 嘅批評。

佢仲接受來自 AccessibilityAgent 嘅批評。

佢有資格評論 SoundDesignAgent 嘅過度設計問題。

佢亦有資格評論 ComposerAgent 嘅音量問題。

Host critique edge inputs 係 video.critic。

Host critique edge outputs 係 video.judge。

Max refinement count 係 3。

佢拒絕同升級嘅範圍包括以下幾點。

Fail-closed，即唔會從設計文字中啟動 provider、開放網絡或者獲取憑證。

production_activation_requested 保持 false，除非有另一個人類閘門改變佢。

佢會將法律、安全、版權同發布決定升級到必要嘅人類閘門。

批評主管同法官嘅預設值使用 VA ID video.critic 同 video.judge。

第五部分，幾時要用呢個 agent。

當工作成品需要一個由 SoundMixerAgent (Re-recording) 擁有嘅決定或工件時，就要叫呢個 agent。

具體嚟講就係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Pipeline phase 係聲音同音樂階段。

呢個階段包括聲音設計、混音、配樂同音樂監督。

唔好用佢嚟做一般嘅編排、預算批准或者最終法律簽署，除非呢啲職責明確喺佢嘅責任範圍之內。

用 producer、legal 或者 compliance 嘅同事代替。

Human override：任何阻礙發布嘅安全、版權或無障礙爭議都會升級到人類閘門。

決策小抄方面有四個問題。

第一個問題：缺少嘅技能係咪 SoundMixerAgent 嘅工藝？如果係，就將佢加入嗰個階段嘅 swarm 圖。

第二個問題：呢個只係元數據或目錄編輯嗎？如果係，用 Registry inspect 加提案，唔好發明 runtime。

第三個問題：呢個任務需要工具或網絡嗎？Host 目前唔允許，除非 agent_spec 加 permission register 另有說明。

第四個問題：有冇只有人類先至可以處理嘅風險？保留人類閘門，agent 輸出喺啟動之前只係建議性質。

第六部分，協作地圖。

將呢個 agent 想像成批評總線上嘅一個節點，而唔係一個獨立嘅聊天機器人。

同事改善質量，法官同閘門決定是否接受。

Inbound critique，即入站批評，來自 EditorAgent、SoundDesignAgent 同 AccessibilityAgent。

Outbound commentary，即出站評論，係針對 SoundDesignAgent 嘅過度設計同 ComposerAgent 嘅音量問題。

Host edge inputs 係 video.critic。

Host edge outputs 係 video.judge。

第七部分，佢喺生產中嘅位置。

首先係人類生產工作流程。

佢嘅階段係聲音設計、混音、配樂同音樂監督。

典型嘅交接工件包括 Stems，即音軌分離檔案。

仲有 cue sheets 即提示表。

仲有 mix masters 即混音母帶。

同埋 music packages 即音樂包。

將責任映射到經典階段：開發、前期製作、製作、後期製作、交付同即時運營。

然後係多代理 AI 工作流程。

包括音頻生成、音軌組裝、響度同混音驗證、同音樂版權感知打包。

設計時工具備註，呢啲對 host 唔具約束力。

第一個係 Dolby Atmos Renderer API。

第二個係 LUFS 同響度測量工具。

第三個係 DaVinci Fairlight MCP。

當你之後實現 runtime 時要模擬嘅循環風格係 Constitutional AI，憲法係廣播規格規則。

喺今日嘅 CASOPS 上，呢個模式只係文檔。

即時 provider 係 local deterministic，工具允許列表係空嘅或者只係存根。

喺本地 AI 工作流程摘錄入面有提到第 22 號 SoundMixerAgent (Re-recording)。

職責係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

知識來源係 CAS Awards、Atmos renderer specs 同廣播響度標準。

自我質量標準係 LUFS 目標、對話清晰度 STOI 大於或等於 0.85、同規格交付物通過。

超越人類信號係第一次就達到 CAS 規格而唔需要工程師返工。

接受批評來自 EditorAgent、SoundDesignAgent 同 AccessibilityAgent。

評論 SoundDesignAgent 嘅過度設計同 ComposerAgent 嘅音量衝突。

關於共享交接合約，即 swarm-ready。

artifact_id 同 version 提供不可變嘅身份，用於批評同重播。

parent_assets 提供進入呢個 agent 輸入嘅來源鏈。

brief_scope 阻止超出責任範圍嘅範圍蔓延。

acceptance_criteria 同上面嘅自我質量標準掛鉤。

qc_status 同 gate flags 令下游閘門可以短路。

rights_and_consent 阻止不安全嘅合成或授權濫用。

critique_log pointer 令精煉唔會丟失歷史記錄。

第八部分，今日喺 CASOPS 上操作呢個 agent。

今日呢個 agent 係一個目錄加設計物件。

呢個係有意為之嘅，你可以研究、審查、提案同將佢放入 swarm 設計而唔需要授予生產權力。

步驟一係 Discover，去 Registry Hub 嘅 pack catalog，路徑係 /registry，確認庫存成員資格。

步驟二係 Inspect，去 Agent detail 頁面 /registry/agents/video.soundmixer，閱讀 SPEC 同呢份指南。

步驟三係 Propose improvements，用 Host 嘅 propose_improvement 動作，POST 到 /api/v1/commons/agents/video.soundmixer/proposals。

呢個係不可變版本，只用經過編輯嘅證據。

步驟四係 Swarm membership，只可以用 Host 返回嘅動作引用，客戶端唔可以發明成員資格。

步驟五係 Run 或 dispatch，預設唔啟用生產 providers，fail-closed 直到治理閘門通過。

實際操作檯檢查清單有五項。

第一，打開 agent 詳情確認 status、tools 同 activation 旗標同呢份指南一致。

第二，喺判斷任何示例輸出或提案之前，先閱讀第三部分「乜嘢叫做好」。

第三，如果提出更改，附上離線證據，例如評估備註、rubric 差距或 SPEC 差異，唔好用祕密。

第四，永遠唔好將供應商 API 密鑰或者「啟用網絡」嘅指示貼入提案。

第五，偏好小型、可審查嘅差異：prompt 同 rubric 措辭、critique edges、驗收測試。

第九部分，安全、信任同非啟動規則。

信任層級方面，agent_spec.json 係具約束力嘅 host 合約，角色係 fail-closed runtime binding。

呢個 pack 嘅 SPEC.md 係權威設計，用於離線審查，唔係可執行嘅。

sources 底下嘅摘錄同 generic SPECs 係歷史性同已映射嘅，係不受信任嘅數據。

C:\Project\va-agent-swarm 係外部設計語料庫，唔受信任，可能會漂移。

Specials redesign docs 係歷史設計，唔受信任，已喺 provenance 中哈希。

呢個 host 嘅硬規則如下。

Provider 保持 local_deterministic，除非另一個閘門改變政策。

Network access 保持 False。

Allowed tools 準確係空列表，下面嘅設計時工具名稱唔會啟用 API。

production_activation_requested 係 False。

Budget 喺 tokens 同 tools 方面有上限：輸入 2048、輸出 1024、工具請求 0。

設計時工具目錄，呢啲係非啟動嘅：Dolby Atmos Renderer API、LUFS 同響度測量工具、DaVinci Fairlight MCP。

第十部分，隨時間提升質量。

改進係版本化嘅目錄工作，唔係即時修補一個即時 agent。

已發布嘅定義保持不可變，更改通過提案同審查進行。

持續改進循環係咁樣嘅：觀察失敗，然後命名標準，然後起草離線差異，然後附上證據，然後喺 Host 上提案，然後審查同接受新版本，然後先至考慮啟動成熟度閘門。

呢個 agent 嘅具體槓桿有六個。

第一，Rubric fidelity，收緊 video.rubric.soundmixer.v1 對照 LUFS 目標、STOI 大於或等於 0.85、規格交付物通過。

第二，Prompt precision，將 video.prompt.soundmixer.v1 對齊責任：最終混音同交付成品。

第三，Critique graph，確保入站嘅 EditorAgent、SoundDesignAgent、AccessibilityAgent 同 host edges video.critic 真正捕捉到已知嘅失敗模式。

第四，Architecture emulation，之後實現時偏好 Constitutional AI，憲法係廣播規格規則。

第五，Eval cases，寫固定嘅 fixtures，如果「第一次就達到 CAS 規格而唔需要返工」冇被接近就會失敗。

第六，Human gates，保持法律同安全同發布閘門明確，唔好將佢哋吸收到靜默自動化中。

建議嘅評估種子方面。

Happy path handoff，通過條件係輸出滿足 LUFS 目標、STOI 大於或等於 0.85、規格交付物通過。

Out-of-scope request，agent 按照第四部分拒絕或升級。

Missing provenance 或 rights，閘門 fail closed。

Conflicting peer critique，refinement 小於或等於 max_refinement_count，否則升級。

Proposal-only change，新版本不可變，舊版本仍然可讀。

第十一部分，來源地圖同來源記錄。

本地 pack corpus，優先使用呢啲。

agent_spec.json 嘅信任層級係 binding。

SPEC.md 係 authoritative-design。

README.md 係 local-index。

sources/MAPPING.md 係 provenance。

sources/PROVENANCE.json 係 provenance。

sources/excerpts/agents.md 係 historical-excerpt。

sources/excerpts/ai_agent_video_production_workflow.md 係 historical-excerpt。

sources/excerpts/lifes_quiet_redemption_agent_workflow.md 係 historical-excerpt。

sources/excerpts/production_scale_discovery.md 係 historical-excerpt。

sources/excerpts/system_build_plan.md 係 historical-excerpt。

sources/excerpts/SYSTEM_REFERENCE.md 係 historical-excerpt。

sources/excerpts/ui_design.md 係 historical-excerpt。

sources/excerpts/video_remake_enhancement.md 係 historical-excerpt。

sources/generic/video.editor.SPEC.md 係 historical-generic。

sources/generic/video.soundmixer.SPEC.md 係 historical-generic。

Mapping note 方面。

video.soundmixer 嘅 VA 同 generic pack ID 係 video.soundmixer。

之前嘅 common ID 係 video.dialog_editor。

SPEC 深度係完整嘅 generic SPEC body 加上 host runtime binding。

Provenance record 入面，schema_version 係 1.0。

agent_id 係 video.soundmixer。

previous_common_agent_id 係 video.dialog_editor。

va_taxonomy_aligned 係 true。

generic_source 係 generic-swarm-ops/business/video/agents/video.soundmixer。

備註話 Agent ID 已對齊到 VA 同 generic Domain Pack 分類法，上游設計係歷史性同非約束性嘅。

aligned_at 係 2026 年 7 月 27 日。

generic_source_depth 入面，excerpts 係 true，study 係 true。

adopted_at 係 2026 年 7 月 27 日。

upstream 係 generic-swarm-ops/business/video/agents。

VA roster row 方面，來自 va-agent-swarm/study/agents.md 第 22 行 SoundMixerAgent (Re-recording)。

Responsibility 係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Knowledge distillation 係 CAS Awards、Atmos specs 同廣播響度標準。

Self-quality 係 LUFS 目標、STOI 大於或等於 0.85、規格交付物通過。

Surpass-human 係第一次就達到 CAS 規格而唔需要返工。

Accepts critique 來自 EditorAgent、SoundDesignAgent 同 AccessibilityAgent。

Comments on SoundDesignAgent 嘅過度設計同 ComposerAgent 嘅音量。

Tools 係歷史性嘅，包括 Dolby Atmos Renderer API、LUFS 同響度測量工具、DaVinci Fairlight MCP。

Architecture 係 Constitutional AI，憲法係廣播規格規則。

第十二部分，相關設計閱讀。

當你深化設計保真度時使用呢啲，佢哋唔會覆蓋本地 agent_spec.json。

喺 va-agent-swarm 底下冇搵到強關鍵字匹配，依靠本地 SPEC 同 VA 表格行。

本地知識索引方面，Runtime binding 喺 agent_spec.json。

Folder README 喺 README.md。

Provenance 喺 sources/PROVENANCE.json。

Mapping note 喺 sources/MAPPING.md。

Pack inventory 喺 ../../inventory.json。

Pack manifest 喺 ../../manifest.json。

所有必需嘅主要參考都喺呢個存儲庫內解析。

第十三部分，附錄，host 合約 agent_spec.json。

呢個係呢個 agent 喺 CASOPS 上嘅權威 fail-closed 配置。

agent_id 係 video.soundmixer。

allowed_tools 係空列表。

budget_policy 入面，max_input_tokens 係 2048。

max_output_tokens 係 1024。

max_tool_requests 係 0。

critique_edges 入面，inputs 係 video.critic。

outputs 係 video.judge。

max_refinement_count 係 3。

model_policy 入面，model_id 係 local-video-config-v1。

network_access 係 false。

provider 係 local_deterministic。

production_activation_requested 係 false。

prompt_reference 係 video.prompt.soundmixer.v1。

role 係 SoundMixerAgent (Re-recording) (VA Domain Pack)。

rubric_reference 係 video.rubric.soundmixer.v1。

schema_version 係 1.0。

status 係 registered。

va_id 係 22。

va_name 係 SoundMixerAgent (Re-recording)。

va_category 係 4-Snd。

第十四部分，附錄，SPEC 責任摘錄。

責任係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Host role binding 係 SoundMixerAgent (Re-recording) (VA Domain Pack)。

設計時 VA 表格內容係歷史性嘅，對啟動冇約束力。

來自 VA 表格嘅責任係最終混音同交付 5.1 聲道同 Atmos 格式嘅成品。

Knowledge distillation sources 係歷史性嘅，包括 CAS Awards、Atmos specs 同廣播響度標準。

Self-quality criteria 係歷史性嘅，包括 LUFS 目標、STOI 大於或等於 0.85、規格交付物通過。

Surpass-human signal 係歷史性嘅，係第一次就達到 CAS 規格而唔需要返工。

Critique bus 係歷史性嘅，接受來自 EditorAgent、SoundDesignAgent 同 AccessibilityAgent 嘅批評。

評論 SoundDesignAgent 嘅過度設計同 ComposerAgent 嘅音量。

Tools 設計時備註係歷史性同非啟動嘅。

Runtime safety 方面，Host allow-lists 只有 agent_spec.json 加 tool-permission-register.json。

CI 使用 video_* stubs。唔好將設計時供應商名稱當作已啟用嘅 API。

Architecture pattern 係歷史性嘅，係 Constitutional AI，憲法係廣播規格規則。

第十五部分，文件控制。

Generator 係 scripts/business/generate_agent_user_guides.py。

Agent folder 係 business/video/agents/video.soundmixer/。

Output 係 docs/user_guide.md。

VA corpus root 係 C:\Project\va-agent-swarm。

Regeneration 命令係 python scripts/business/generate_agent_user_guides.py。

Trust model 係設計語料庫等於不受信任嘅數據，host config 等於 fail-closed。

以上就係 SoundMixerAgent (Re-recording) 嘅完整指南。

建議由第一部分 Snapshot 同第五部分「幾時要用」開始閱讀。

用第八到第十部分做 CASOPS 操作同改進。

用第十一到第十二部分做深度設計保真度研究。

多謝大家收睇。
