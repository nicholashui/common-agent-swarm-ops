# CommunityAgent — Operator & Design Guide

> **Who this is for:** operators, pack authors, swarm designers, and reviewers working in `common-agent-swarm-ops`.
> **What you will get:** mission clarity, when-to-use guidance, collaboration map, CASOPS operating steps, improvement path, and a trust-tiered source map.
>
> **What this is not:** a license to activate providers, open network access, or invent tools. Host truth is fail-closed `agent_spec.json`. Design text from VA / redesign docs is **untrusted provenance**.

*Pack path:* `business/video/agents/video.community/` · *Guide:* `docs/user_guide.md` · *Generated by* `scripts/business/generate_agent_user_guides.py`

## 1. Snapshot

**CommunityAgent** (`video.community`) is a Video Domain Pack role on CASOPS. In plain terms: Captures community response and triages qualitative signals. Treat this guide as the human-readable operator map for catalog review, swarm design, and proposal-driven improvement — not as a runtime activation license.

| | |
|---|---|
| **Display name** | CommunityAgent |
| **Pack agent id** | `video.community` |
| **Pack** | `video` |
| **Host role string** | CommunityAgent (VA Domain Pack) |
| **Catalog status** | `registered` |
| **Maturity today** | L0 / non-active (catalog & design fidelity) |
| **VA id** | `88` |
| **VA category** | `10-Sup` — Workflow support & release gates |
| **Provider (host)** | `local_deterministic` |
| **Network** | `False` |
| **Allowed tools (host)** | `[]` |
| **Production activation requested** | `False` |
| **Prompt / rubric refs** | `video.prompt.community.v1` / `video.rubric.community.v1` |
| **Previous common id** | `video.community_manager` |
| **Generic lineage** | `generic-swarm-ops/business/video/agents/video.community` |

## 2. Why this role exists

Human video crews already solve this problem under time pressure: Captures community response and triages qualitative signals. The agent form exists so the multi-agent system can **encode that craft continuously**, apply it on every handoff, and escalate only the residual risk to humans.

Its knowledge base is distilled from: **Community moderation playbooks, sentiment datasets, escalation rules** — historical design sources, not live network calls on this host.

The design ambition (surpass-human signal) is: *Surfaces emerging audience concerns earlier than manual comment review.* Use that as the north-star metric when you write evals or proposals later.

## 3. What good looks like

Use these as **review criteria** when reading outputs, writing rubrics, or scoring proposals.

- **Primary outcome:** Captures community response and triages qualitative signals.
- **Self-quality bar:** Response latency, issue clustering quality, sentiment tracking accuracy
- **Ambition signal (design-time):** Surfaces emerging audience concerns earlier than manual comment review
- **Architecture pattern (design-time):** Reflexion from post-launch audience feedback

### Host quality & critique binding

- Prompt reference: `video.prompt.community.v1`
- Rubric reference: `video.rubric.community.v1`
- Critique edges: `{"inputs": ["video.critic"], "outputs": ["video.judge"]}`
- Max refinement: `3`
- VA table quality criteria retained under Provenance and Identity surface above.

### Inputs → outputs → acceptance

- Inputs: local pack artifacts and typed handoffs.
- Outputs: reviewable video-domain deliverables with acceptance criteria.
- Acceptance: host policy plus local SPEC criteria; no external repository required.

## 4. Scope: owns, shares, and refuses

### Owns

Captures community response and triages qualitative signals.

### Shares with peers (critique surface)

- **Listens to / accepts critique from:** AnalystAgent, SocialMediaStrategistAgent, CommsAgent
- **Is qualified to comment on:** Confusing messaging, sentiment risks, recurring complaints
- **Host critique edge inputs:** `['video.critic']`
- **Host critique edge outputs:** `['video.judge']`
- **Max refinement count:** `3`

### Refuses / escalates

- Fail-closed: no provider activation, no network, no credentials from design text.
- `production_activation_requested` remains false unless a separate human gate changes it.
- Escalates legal, safety, rights, and release decisions to required human gates.
- Critique lead / judge defaults use VA IDs `video.critic` and `video.judge`.

## 5. When to involve this agent

- **Call this agent when** the work product needs a decision or artifact owned by *CommunityAgent*: Captures community response and triages qualitative signals
- **Pipeline phase:** Workflow support & release gates. Support functions and final release gates (accessibility, brand, legal, archive, etc.).
- **Do not call it for** generic orchestration, budget greenlight, or final legal sign-off unless those duties are explicitly in its responsibility — use producer/legal/compliance peers instead.
- **Human override:** any release-blocking safety, rights, or accessibility dispute escalates to a human gate even if the agent would auto-pass in a future active runtime.

### Decision cheat-sheet

| Question | Guidance |
|----------|----------|
| Is the missing skill *CommunityAgent*'s craft? | Yes → include in the swarm graph for that phase. |
| Is this only a metadata/catalog edit? | Use Registry inspect + proposal — do not invent runtime. |
| Does the task need tools/network? | Host currently disallows unless `agent_spec` + permission register say otherwise. |
| Is there a human-only risk (legal, harm, rights)? | Keep a human gate; agent output is advisory until activated. |

## 6. Collaboration map

Think of this agent as a **node on a critique bus**, not a standalone chatbot. Peers refine quality; judges/gates decide acceptance.

| Direction | Agents / topics |
|-----------|-----------------|
| **Inbound critique** | AnalystAgent, SocialMediaStrategistAgent, CommsAgent |
| **Outbound commentary** | Confusing messaging, sentiment risks, recurring complaints |
| **Host edge inputs** | ['video.critic'] |
| **Host edge outputs** | ['video.judge'] |

## 7. Where it sits in production

### 7.1 Human production workflow

Support functions and final release gates (accessibility, brand, legal, archive, etc.).

**Typical handoff artifacts:** Gate decisions, compliance packs, accessibility layers, archive masters, corrections.

**Mentions in local human-workflow excerpt:**

- 43 — **Sign-Language Interpreter** — Accurate ASL/BSL interpretation — Certified (RID NIC / NAD), cultural fluency — Interpreter training + 5+ yrs certified work — Accessibility avatars, news, edu — National Association of the Deaf (NAD) reviewers; methods: RID CPC ethics review, Deaf community panel feedback
- 46 — **AI Prompt Engineer / Generator Operator** — Crafts prompts and steers generative video models — Model literacy, iterative prompting, taste, seed/control tooling — 1–3 yrs hands-on with Sora/Veo/Runway/Kling + visual background — All AI-generated formats — Karen X. Cheng, Paul Trillo, Don Allen Stevenson III, Nik Kleverov; methods: AI Film Festival (Runway) jury, r/aivideo community critique
- 6. Post-launch — 24-hr retention, hold-rate, comments — Data Analyst, Community Manager — TikTok Analytics, Meta Insights — Iteration notes for next post — Daily for 7 days

### 7.2 Multi-agent AI workflow

Gate and support nodes that accept/reject handoffs before publish or after launch.

Design-time tool notes (non-binding on host): Social listening tools, moderation dashboards, clustering models

Loop style to emulate when you later implement runtime: **Reflexion from post-launch audience feedback**. On CASOPS today, that pattern is documentation only — the live provider is local deterministic with empty or stub tool allow-lists.

**Mentions in local AI-workflow excerpt:**

- 43 — **SignLanguageInterpreterAgent** — Accurate ASL/BSL interpretation — RID NIC curricula; NAD-endorsed corpora; Deaf-community consented sign data — Sign accuracy (Deaf-reviewer vote); facial-grammar markers — Wins blind NAD-reviewer preference at scale — DeafCommunityReviewAgent (HiTL), LinguistAgent — VoiceCloneAgent (no caption), AccessibilityAgent
- 46 — **PromptEngineerAgent / GeneratorOperator** — Crafts prompts; steers Sora/Veo/Runway/Kling — Karen X. Cheng / Paul Trillo public prompt sets; r/aivideo community; Runway AIFF jury notes — Prompt→output CLIP-T score; iteration count to acceptance; seed-control reproducibility — Hits target shot in ≤3 iterations vs human's avg of 10 — DirectorAgent, AIQAAgent — AIQAAgent (re-roll budget), Consi
- 78 — **AccessibilityOptimizerAgent** — WCAG 2.2 contrast, caption timing, audio description quality, color-blind safe palette — WCAG 2.2 spec; W3C/WAI-ARIA; DCMP captioning key; Deaf/HoH community guidelines — WCAG-conformance score 100% AA, ≥90% AAA; caption WER ≤2% — Catches more a11y defects than ADA-certified human auditor — AccessibilityAgent (HiTL), ComplianceAgent — EditorAgent (caption syn

### 7.3 Shared handoff contract (swarm-ready)

When you wire this agent into a future active swarm, require machine-readable manifests between nodes:

| Field | Why it matters for this role |
|-------|------------------------------|
| `artifact_id` / version | Immutable identity for critique and replay |
| `parent_assets` | Provenance chain into this agent's inputs |
| `brief_scope` | Stops scope creep outside responsibility |
| `acceptance_criteria` | Ties to self-quality bar above |
| `qc_status` / gate flags | Lets downstream gates short-circuit |
| `rights_and_consent` | Blocks unsafe synthetic or licensed misuse |
| `critique_log` pointer | Enables refinement without losing history |

## 8. Operating this agent on CASOPS today

Today the agent is a **catalog + design object**. That is intentional: you can study, review, propose, and place it in swarm designs without granting production power.

| Step | What to do | Why |
|------|------------|-----|
| 1. Discover | Registry Hub → pack catalog (`/registry`) | Confirm inventory membership |
| 2. Inspect | Agent detail `/registry/agents/video.community` | Read SPEC / this guide as markdown |
| 3. Propose improvements | Host `propose_improvement` action → `POST /api/v1/commons/agents/video.community/proposals` | Immutable versions; redacted evidence only |
| 4. Swarm membership | Only with Host-returned action references | Client must not invent membership |
| 5. Run / dispatch | Not enabled for production providers by default | Fail-closed until governance gate |

### Practical console checklist

1. Open the agent detail and confirm **status**, **tools**, and **activation** flags match this guide.
2. Read **§3 What good looks like** before judging any sample output or proposal.
3. If proposing a change, attach offline evidence (eval notes, rubric gap, SPEC delta) — no secrets.
4. Never paste vendor API keys or “enable network” instructions into proposals.
5. Prefer small, reviewable deltas: prompt/rubric wording, critique edges, acceptance tests.

## 9. Safety, trust, and non-activation rules

| Layer | Trust | Role |
|-------|-------|------|
| `agent_spec.json` | **Binding host contract** | Fail-closed runtime binding |
| This pack `SPEC.md` | Authoritative **design** for offline review | Not executable |
| `sources/*` excerpts & generic SPECs | Historical / mapped | Untrusted data |
| `C:\Project\va-agent-swarm` | External design corpus | Untrusted; may drift |
| Specials redesign docs | Historical design | Untrusted; hashed in provenance |

**Hard rules on this host:**

- Provider stays `local_deterministic` unless a separate gate changes policy.
- Network access stays `False`.
- Allowed tools are exactly `[]` — design-time tool names below do **not** enable APIs.
- `production_activation_requested` is `False`.
- Budget (tokens/tools) is capped: in `2048` / out `1024` / tool requests `0`.
- Design-time tool catalogue (non-activating): Social listening tools, moderation dashboards, clustering models

## 10. Raising quality over time

Improvement is **versioned catalog work**, not hot-patching a live agent. Published definitions stay immutable; changes go through proposals and review.

### 10.1 Continuous improvement loop

```text
Observe failure → Name the criterion (from §3) → Draft offline delta
    → Attach evidence → Propose on Host → Review / accept new version
    → Only then consider activation maturity gates
```

### 10.2 Concrete levers for this agent

1. **Rubric fidelity** — tighten `video.rubric.community.v1` against: Response latency, issue clustering quality, sentiment tracking accuracy
2. **Prompt precision** — align `video.prompt.community.v1` with responsibility: Captures community response and triages qualitative signals.
3. **Critique graph** — ensure inbound `AnalystAgent, SocialMediaStrategistAgent, CommsAgent` and host edges `['video.critic']` actually catch known failure modes.
4. **Architecture emulation** — when implementing later, prefer: Reflexion from post-launch audience feedback
5. **Eval cases** — write fixed fixtures that would fail if *Surfaces emerging audience concerns earlier than manual comment review* is not approached.
6. **Human gates** — keep legal/safety/release gates explicit; do not absorb them into silent automation.

### 10.3 Suggested evaluation seeds

| Seed | Pass condition |
|------|----------------|
| Happy path handoff | Output satisfies: Response latency, issue clustering quality, sentiment tracking accuracy |
| Out-of-scope request | Agent refuses or escalates per §4 |
| Missing provenance / rights | Gate fails closed |
| Conflicting peer critique | Refinement ≤ max_refinement_count; else escalate |
| Proposal-only change | New version immutable; old version still readable |

## 11. Source map and provenance

### 11.1 Local pack corpus (prefer these first)

| Path | Trust tier |
|------|------------|
| `agent_spec.json` | `binding` |
| `SPEC.md` | `authoritative-design` |
| `README.md` | `local-index` |
| `sources/MAPPING.md` | `provenance` |
| `sources/PROVENANCE.json` | `provenance` |
| `sources/excerpts/agents.md` | `historical-excerpt` |
| `sources/excerpts/ai_agent_video_production_workflow.md` | `historical-excerpt` |
| `sources/excerpts/human_video_production_workflow.md` | `historical-excerpt` |
| `sources/excerpts/lifes_quiet_redemption_agent_workflow.md` | `historical-excerpt` |
| `sources/excerpts/production_scale_discovery.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.3.md` | `historical-excerpt` |
| `sources/excerpts/project_starter_0.5.md` | `historical-excerpt` |
| `sources/excerpts/RETHINK_100_IMPROVEMENTS.md` | `historical-excerpt` |
| `sources/excerpts/video_generation_techology_should_learn_now.md` | `historical-excerpt` |
| `sources/generic/video.community.SPEC.md` | `historical-generic` |

### 11.2 Mapping note

# Mapping — `video.community` - VA/generic pack ID: `video.community` - Previous common ID: `video.community_manager` - SPEC depth: full generic SPEC body + host runtime binding

### 11.3 Provenance record

```json
{
  "schema_version": "1.0",
  "agent_id": "video.community",
  "previous_common_agent_id": "video.community_manager",
  "va_taxonomy_aligned": true,
  "generic_source": "generic-swarm-ops/business/video/agents/video.community",
  "note": "Agent ID aligned to VA/generic Domain Pack taxonomy. Upstream design is historical and non-binding.",
  "aligned_at": "2026-07-27T00:48:10.891368Z",
  "generic_source_depth": {
    "excerpts": true,
    "study": true,
    "adopted_at": "2026-07-27T01:30:27.883468+00:00",
    "upstream": "generic-swarm-ops/business/video/agents"
  }
}
```

### 11.4 Provenance narrative (SPEC)

- Pack agent ID `video.community` is aligned to pure VA Domain Pack / generic pack taxonomy (Agent IDs ≈ VA tables). - Previous common inventory ID `video.community_manager` is historical mapping only. - Upstream design body below is **historical and non-binding**; local `agent_spec.json` is authoritative. - Full VA/generic SPEC depth retained for offline design fidelity (including category roster rows and common agent structure when present upstream).

### 11.5 VA roster row (historical design)

From `va-agent-swarm/study/agents.md` row **#88 CommunityAgent** (design corpus — non-binding for activation).

| Dimension | Content |
|-----------|---------|
| Responsibility | Captures community response and triages qualitative signals |
| Knowledge distillation | Community moderation playbooks, sentiment datasets, escalation rules |
| Self-quality | Response latency, issue clustering quality, sentiment tracking accuracy |
| Surpass-human | Surfaces emerging audience concerns earlier than manual comment review |
| Accepts critique | AnalystAgent, SocialMediaStrategistAgent, CommsAgent |
| Comments on | Confusing messaging, sentiment risks, recurring complaints |
| Tools (historical) | Social listening tools, moderation dashboards, clustering models |
| Architecture | Reflexion from post-launch audience feedback |

## 12. Related design reading

Use these when deepening design fidelity. They do **not** override local `agent_spec.json`.

### 12.1 Files under `C:\Project\va-agent-swarm`

| File | Why it may matter |
|------|-------------------|
| `study/podcast_agent_functional_specifcation.md` | # Podcast Production and AI Agent Systems ## Part 1: Traditional Podcast Host Workflow The workflow of a podcast host is a precise and multi-layered creative process that requires a combination of creative thinking, technical expertise, and real-time adaptability. (score 2) |
| `study/ui/production_scale_discovery_hk.md` | # 製作規模探索 — 圖庫、示範與智能選擇 > 系統如何幫助使用者理解自己想做甚麼、 > 瀏覽真實例子，並在承諾預算或啟動代理人之前， > 選出合適的製作規模。 --- ## 問題 --- ## 解法：Discovery Hub（新增一個頂層區段） --- ## 頁面：Discovery Hub ```text ┌─────────────────────────────────────────────────────────────────── (score 2) |

### 12.3 Local knowledge index (SPEC)

- [Runtime binding](agent_spec.json)
- [Folder README](README.md)
- [Provenance](sources/PROVENANCE.json)
- [Mapping note](sources/MAPPING.md)
- [Pack inventory](../../inventory.json)
- [Pack manifest](../../manifest.json)
- All required primary references resolve inside this repository.

### 12.4 Folder README (summary)

VA-aligned Domain Pack agent (common host). | File | Purpose | |------|----------| | `SPEC.md` | VA/generic depth + host binding | | `agent_spec.json` | Fail-closed runtime | | `sources/` | Provenance / mapping |

## 13. Appendix — host contract (`agent_spec.json`)

Authoritative fail-closed configuration for this agent on CASOPS:

```json
{
  "agent_id": "video.community",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.community.v1",
  "role": "CommunityAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.community.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 88,
  "va_name": "CommunityAgent",
  "va_category": "10-Sup"
}
```

<details><summary>SPEC runtime binding prose</summary>

Authoritative fail-closed host configuration:

```json
{
  "agent_id": "video.community",
  "allowed_tools": [],
  "budget_policy": {
    "max_input_tokens": 2048,
    "max_output_tokens": 1024,
    "max_tool_requests": 0
  },
  "critique_edges": {
    "inputs": [
      "video.critic"
    ],
    "outputs": [
      "video.judge"
    ]
  },
  "max_refinement_count": 3,
  "model_policy": {
    "model_id": "local-video-config-v1",
    "network_access": false,
    "provider": "local_deterministic"
  },
  "production_activation_requested": false,
  "prompt_reference": "video.prompt.community.v1",
  "role": "CommunityAgent (VA Domain Pack)",
  "rubric_reference": "video.rubric.community.v1",
  "schema_version": "1.0",
  "status": "registered",
  "va_id": 88,
  "va_name": "CommunityAgent",
  "va_category": "10-Sup"
}
```

</details>

## 14. Appendix — SPEC responsibility excerpt

<details><summary>Expanded responsibility text from local SPEC.md</summary>

Captures community response and triages qualitative signals
Host role binding: `CommunityAgent (VA Domain Pack)`. Design-time VA table content below is historical and non-binding for activation.
### Responsibility (from VA table)
Captures community response and triages qualitative signals
### Knowledge distillation sources (historical)
Community moderation playbooks, sentiment datasets, escalation rules
### Self-quality criteria (historical)
Response latency, issue clustering quality, sentiment tracking accuracy
### Surpass-human signal (historical)
Surfaces emerging audience concerns earlier than manual comment review
### Critique bus (historical)
- **Accepts critique from:** AnalystAgent, SocialMediaStrategistAgent, CommsAgent
- **Comments on:** Confusing messaging, sentiment risks, recurring complaints
### Tools design-time notes (historical, non-activating)
Social listening tools, moderation dashboards, clustering models
**Runtime safety:** Host allow-lists are only `agent_spec.json` + `tool-permission-register.json`. CI uses video_* stubs. Do not treat design-time vendor names as enabled APIs.
### Architecture pattern (historical)
Reflexion from post-launch audience feedback

</details>

## 15. Document control

| Item | Value |
|------|-------|
| Generator | `scripts/business/generate_agent_user_guides.py` |
| Agent folder | `business/video/agents/video.community/` |
| Output | `docs/user_guide.md` |
| VA corpus root | `C:\Project\va-agent-swarm` |
| Regeneration | `python scripts/business/generate_agent_user_guides.py` |
| Trust model | Design corpus = untrusted data; host config = fail-closed |

*End of guide for **CommunityAgent**. Start from §1 Snapshot and §5 When to involve; use §8–§10 for CASOPS operations and improvement; use §11–§12 only for deep design fidelity.*
