# `video.audio_qc_reviewer`

> Self-contained agent for host `common-agent-swarm-ops`.

| File | Purpose |
|------|----------|
| `SPEC.md` | Full offline role definition |
| `agent_spec.json` | Host runtime binding (non-active) |
| `sources/` | Provenance + mapping notes for audit |
| `prompts/` | Optional prompt stubs (host prompt_reference) |
| `rubrics/` | Optional rubric stubs |

Open this folder alone — no external repo or pack `corpus/` is required.
