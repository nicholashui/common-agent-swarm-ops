呢個係 LocalizationQAAgent，即係語言學家角色嘅操作同設計指南完整旁白。

呢份指南係寫畀操作員、pack 作者、swarm 設計師同埋喺 common-agent-swarm-ops 入面做審核嘅人睇嘅。

你會得到任務清晰度、幾時用呢個 agent 嘅指引、協作地圖、CASOPS 操作步驟、改進路徑同埋一個分信任層級嘅來源地圖。

呢份文件唔係一個啟動 provider 嘅授權，唔係開放網絡存取嘅許可，亦唔係可以自己發明工具嘅許可。Host 嘅真相係 fail-closed 嘅 agent_spec.json。來自 VA 或者 redesign 文檔嘅設計文字屬於不受信任嘅來源。

Pack 路徑係 business/video/agents/video.localizationqa/。指南位置係 docs/user_guide.md。呢個係由 scripts/business/generate_agent_user_guides.py 自動生成嘅。

第一部分，快照。

LocalizationQAAgent，括號入面寫住 Linguist，佢嘅 pack agent id 係 video.localizationqa，係 Video Domain Pack 喺 CASOPS 上面嘅一個角色。簡單嚟講就係做翻譯加文化適配。你應該將呢份指南當成人類可讀嘅操作地圖，用嚟做目錄審核、swarm 設計同埋建議驅動嘅改進，而唔係當成運行時啟動嘅許可證。

而家逐個欄位講解快照表格入面嘅資料。

顯示名稱係 LocalizationQAAgent，括號 Linguist。

Pack agent id 係 video.localizationqa。

所屬嘅 Pack 係 video。

Host role string 係 LocalizationQAAgent (Linguist) (VA Domain Pack)。

Catalog 狀態係 registered，即係已經註冊。

今日嘅成熟度係 L0，即非活躍狀態，只有目錄同設計保真度。

VA id 係 44。

VA 類別係 7-Edu，代表教育同領域專業知識。

Provider，即主機供應商，係 local_deterministic。

網絡存取係 False，即係冇網絡權限。

Host 允許嘅工具係空嘅，即係一個空白嘅列表。

Production activation requested 係 False，即係未有請求生產啟動。

Prompt 同 rubric 嘅參考分別係 video.prompt.localizationqa.v1 同 video.rubric.localizationqa.v1。

之前嘅 common id 係 video.localization_specialist。

Generic lineage 係 generic-swarm-ops/business/video/agents/video.localizationqa。

第二部分，點解呢個角色存在。

人類影片團隊已經喺時間壓力之下解決緊呢個問題，就係翻譯加文化適配。Agent 形式存在嘅原因係多代理系統可以持續編碼呢門手藝，喺每一次交接嘅時候應用，只將剩餘風險升級畀人類。

佢嘅知識庫係從以下來源提煉嘅：LISA QA 模型、MQM 錯誤分類法、ATA 認證備考。呢啲係歷史設計來源，唔係喺呢個 host 上面嘅即時網絡調用。

設計野心，即超越人類嘅信號，係：喺 MQM 評分上以十倍速度打敗 LSP 人類 QA。當你之後寫評估或者建議嘅時候，用呢個做北極星指標。

第三部分，乜嘢算做得好。

呢啲係你閱讀輸出、撰寫評分標準或者評分建議嘅時候應該用嘅審核標準。

主要成果係翻譯加文化適配。

自我質量標準係每一千字嘅 MQM 錯誤數同埋文化標記計數。

野心信號，即設計時嘅目標，係以十倍速度喺 MQM 上面打敗 LSP 人類 QA。

架構模式，設計時嘅，係 Self-Refine，括號入面寫住 rubric 係 MQM scoring framework。

關於 host 質量同批評綁定嘅細節如下。

Prompt 參考係 video.prompt.localizationqa.v1。

Rubric 參考係 video.rubric.localizationqa.v1。

Critique edges 嘅 inputs 係 video.critic，outputs 係 video.judge。

最大精煉次數係 3。

VA 表格嘅質量標準保留喺上面嘅 Provenance 同 Identity surface 之下。

關於輸入、輸出同驗收嘅描述。輸入係本地 pack artifacts 同 typed handoffs。輸出係帶有驗收標準嘅可審核影片領域交付物。驗收係 host policy 加上本地 SPEC 標準，唔需要外部 repository。

第四部分，範圍：擁有、共享同拒絕。

擁有嘅範圍係翻譯加文化適配。

同 peers 共享嘅批評表面如下。

接受批評嘅來源係 NativeReviewerAgent 同 BrandAgent。

有資格評論嘅對象係 VoiceCloneAgent，特別係發音問題，同埋 DubbingAgent。

Host critique edge inputs 係 video.critic。

Host critique edge outputs 係 video.judge。

最大精煉次數係 3。

關於拒絕同升級嘅規則。Fail-closed，即係冇 provider 啟動、冇網絡、冇從設計文字攞到嘅憑證。production_activation_requested 保持 false，除非有另外嘅人類關卡改變佢。法律、安全、權利同發布決定會升級到必要嘅人類關卡。Critique lead 同 judge 默認使用 VA ID video.critic 同 video.judge。

第五部分，幾時要用到呢個 agent。

當工作產品需要 LocalizationQAAgent (Linguist) 擁有嘅決策或者製品嘅時候就要調用佢，即係翻譯加文化適配。

Pipeline 階段係教育同領域專業知識，即主題準確性同教學包裝。

唔好為咗通用編排、預算批准或者最終法律簽署而調用佢，除非呢啲職責明確喺佢嘅責任入面，否則用 producer、legal 或者 compliance peers。

人類覆蓋規則：任何阻止發布嘅安全、權利或者可及性爭議，即使 agent 喺未來嘅活躍運行時會自動通過，都要升級到人類關卡。

決策速查表有四行。

第一行，問題係：缺少嘅技能係唔係 LocalizationQAAgent (Linguist) 嘅手藝？指引係：係嘅話，將佢包含喺嗰個階段嘅 swarm 圖入面。

第二行，問題係：呢個只係元數據或目錄編輯嗎？指引係：用 Registry inspect 加 proposal，唔好發明 runtime。

第三行，問題係：呢個任務需要工具或網絡嗎？指引係：Host 目前唔允許，除非 agent_spec 加 permission register 另外有講。

第四行，問題係：呢度有冇只屬於人類嘅風險，例如法律、傷害、權利？指引係：保留人類關卡；agent 嘅輸出喺啟動之前只係諮詢性質。

第六部分，協作地圖。

將呢個 agent 想像成批評總線上面嘅一個節點，而唔係一個獨立嘅聊天機器人。Peers 精煉質量，judges 同 gates 決定驗收。

入站批評嘅方向：NativeReviewerAgent 同 BrandAgent。

出站評論嘅方向：VoiceCloneAgent 嘅發音問題同 DubbingAgent。

Host edge inputs 係 video.critic。

Host edge outputs 係 video.judge。

第七部分，佢喺生產中嘅位置。

第七點一，人類生產工作流。

屬於主題準確性同教學包裝嘅範疇。

典型嘅交接製品包括：課程包、SME 審核、LMS 捆綁包、評估製品。

將責任映射到經典階段：開發、前期製作、製作、後期製作、交付或即時運營。優先選擇一個擔任呢個職位嘅人會簽署嘅階段。

第七點二，多 agent AI 工作流。

屬於領域根基建立、教學設計、LMS 包裝、學習者模擬嘅範疇。

設計時工具備註，呢啲對 host 冇約束力：DeepL 同 Google Translate API、MQM 錯誤標註器、術語管理即 memoQ API。

之後實現運行時嘅時候要模擬嘅迴圈風格係 Self-Refine，括號入面寫住 rubric 係 MQM scoring framework。喺今日嘅 CASOPS 上面，呢個模式只係文檔，實際嘅 provider 係 local deterministic 配合空白或者 stub 工具允許列表。

本地 AI 工作流摘錄入面提到嘅條目如下。

編號 44，LocalizationQAAgent (Linguist)，負責翻譯加文化適配。知識來源係 LISA QA 模型、MQM 錯誤分類法、ATA 認證備考。質量標準係每一千字 MQM 錯誤率同文化標記計數。超越人類信號係以十倍速度喺 MQM 錯誤率上面打敗 LSP 人類 QA。接受批評嘅來源係 NativeReviewerAgent 同 BrandAgent。評論對象係 VoiceCloneAgent 嘅錯誤發音同 DubbingAgent。

第七點三，共享交接合約，即 swarm-ready。

當你將呢個 agent 接入未來嘅活躍 swarm 嘅時候，需要節點之間有機器可讀嘅清單。

artifact_id 加版本：提供不可變嘅身份用於批評同重放。

parent_assets：進入呢個 agent 輸入嘅來源鏈。

brief_scope：防止責任範圍蔓延到責任之外。

acceptance_criteria：連結到上面嘅自我質量標準。

qc_status 加 gate flags：令下游 gates 可以短路。

rights_and_consent：阻止不安全嘅合成或授權誤用。

critique_log pointer：令精煉過程唔會丟失歷史。

第八部分，今日喺 CASOPS 上面操作呢個 agent。

今日呢個 agent 係一個目錄加設計對象。呢個係刻意嘅：你可以研究、審核、提議同埋將佢放入 swarm 設計，而唔需要授予生產權力。

五個步驟如下。

第一步，發現。去 Registry Hub 嘅 pack catalog，路徑係 /registry。原因係確認庫存成員資格。

第二步，檢查。去 agent 詳情頁面 /registry/agents/video.localizationqa。原因係以 markdown 讀取 SPEC 同呢份指南。

第三步，提議改進。用 Host 嘅 propose_improvement action，即 POST /api/v1/commons/agents/video.localizationqa/proposals。原因係不可變版本，只有經過編輯嘅證據。

第四步，Swarm 成員資格。只能用 Host 返回嘅 action references。原因係客戶端唔可以自己發明成員資格。

第五步，運行或調度。默認唔啟用生產 providers。原因係 fail-closed 直到治理關卡通過。

實用控制檯清單有五項。

第一，打開 agent 詳情確認狀態、工具同啟動 flags 同呢份指南一致。

第二，喺判斷任何樣本輸出或建議之前先閱讀第三部分「乜嘢算做得好」。

第三，如果提議更改，附上離線證據例如評估筆記、rubric 差距、SPEC 差異，但唔好放祕密。

第四，永遠唔好將供應商 API 金鑰或者「啟用網絡」嘅指令貼入建議入面。

第五，優先做細小、可審核嘅改動：prompt 同 rubric 措辭、critique edges、驗收測試。

第九部分，安全、信任同非啟動規則。

信任層級表有五行。

第一行，agent_spec.json，信任級別係有約束力嘅 host 合約，角色係 fail-closed runtime binding。

第二行，呢個 pack 嘅 SPEC.md，信任級別係權威設計用於離線審核，角色係不可執行。

第三行，sources 底下嘅摘錄同 generic SPECs，信任級別係歷史或已映射，角色係不受信任嘅數據。

第四行，C:\Project\va-agent-swarm，信任級別係外部設計語料庫，角色係不受信任，可能會漂移。

第五行，Specials redesign docs，信任級別係歷史設計，角色係不受信任，已喺 provenance 中哈希。

呢個 host 上面嘅硬規則如下。

Provider 保持 local_deterministic 除非另外嘅關卡改變政策。

網絡存取保持 False。

允許嘅工具精確係空列表，下面嘅設計時工具名稱唔會啟用 API。

production_activation_requested 係 False。

預算係有上限嘅：輸入 2048 tokens、輸出 1024 tokens、工具請求 0。

設計時工具目錄，呢啲係非啟動性質嘅：DeepL 同 Google Translate API、MQM 錯誤標註器、術語管理即 memoQ API。

第十部分，隨時間提升質量。

改進係版本化嘅目錄工作，唔係即時修補一個運行中嘅 agent。已發布嘅定義保持不可變，更改要經過建議同審核。

第十點一，持續改進迴圈。

觀察失敗，命名標準即從第三部分取，起草離線差異，附上證據，喺 Host 上提議，審核同接受新版本，之後先考慮啟動成熟度關卡。

第十點二，呢個 agent 嘅具體槓桿有六個。

第一個係 Rubric 保真度。收緊 video.rubric.localizationqa.v1 針對每一千字 MQM 錯誤數同文化標記計數。

第二個係 Prompt 精確度。將 video.prompt.localizationqa.v1 同責任對齊，即翻譯加文化適配。

第三個係 Critique 圖。確保入站嘅 NativeReviewerAgent 同 BrandAgent 以及 host edges video.critic 真正能捕捉到已知嘅失敗模式。

第四個係架構模擬。之後實現嘅時候優先用 Self-Refine，括號 rubric 係 MQM scoring framework。

第五個係評估案例。寫固定嘅 fixtures，如果「以十倍速度喺 MQM 上面打敗 LSP 人類 QA」呢個目標未被接近就會失敗。

第六個係人類關卡。保持法律、安全同發布 gates 明確，唔好將佢哋吸收到靜默自動化入面。

第十點三，建議嘅評估種子有五個。

第一個係快樂路徑交接。通過條件係輸出滿足每一千字 MQM 錯誤數同文化標記計數。

第二個係超出範圍嘅請求。通過條件係 agent 按照第四部分拒絕或升級。

第三個係缺少來源或權利。通過條件係 gate fail-closed。

第四個係衝突嘅 peer 批評。通過條件係精煉次數小於等於 max_refinement_count，否則升級。

第五個係只有建議嘅更改。通過條件係新版本不可變，舊版本仍然可讀。

第十一部分，來源地圖同出處。

第十一點一，本地 pack 語料庫，優先使用呢啲。

agent_spec.json 嘅信任層級係 binding。

SPEC.md 嘅信任層級係 authoritative-design。

README.md 嘅信任層級係 local-index。

sources/MAPPING.md 嘅信任層級係 provenance。

sources/PROVENANCE.json 嘅信任層級係 provenance。

sources/excerpts/agents.md 嘅信任層級係 historical-excerpt。

sources/excerpts/ai_agent_video_production_workflow.md 嘅信任層級係 historical-excerpt。

sources/excerpts/lifes_quiet_redemption_agent_workflow.md 嘅信任層級係 historical-excerpt。

sources/excerpts/SYSTEM_REFERENCE.md 嘅信任層級係 historical-excerpt。

sources/generic/video.localizationqa.SPEC.md 嘅信任層級係 historical-generic。

第十一點二，映射備註。

映射對象係 video.localizationqa。VA 同 generic pack ID 係 video.localizationqa。之前嘅 common ID 係 video.localization_specialist。SPEC 深度係完整嘅 generic SPEC body 加 host runtime binding。

第十一點三，出處記錄。

schema_version 係 1.0。agent_id 係 video.localizationqa。previous_common_agent_id 係 video.localization_specialist。va_taxonomy_aligned 係 true。generic_source 係 generic-swarm-ops/business/video/agents/video.localizationqa。note 寫住 Agent ID aligned to VA/generic Domain Pack taxonomy，上游設計係歷史性同非約束性嘅。aligned_at 係 2026-07-27T00:48:10.712686Z。generic_source_depth 入面，excerpts 係 true，study 係 true，adopted_at 係 2026-07-27T01:30:28.363786+00:00，upstream 係 generic-swarm-ops/business/video/agents。

第十一點四，出處敘述。

Pack agent ID video.localizationqa 係對齊到純 VA Domain Pack 同 generic pack taxonomy 嘅，Agent IDs 約等於 VA 表格。之前嘅 common inventory ID video.localization_specialist 只係歷史映射。上游設計體係歷史性同非約束性嘅，本地 agent_spec.json 係權威嘅。保留完整嘅 VA/generic SPEC 深度用於離線設計保真度，包括類別名冊行同共用 agent 結構。

第十一點五，VA 名冊行，歷史設計。

來自 va-agent-swarm/study/agents.md 第 44 行 LocalizationQAAgent (Linguist)，設計語料庫，對啟動無約束力。

Responsibility 係翻譯加文化適配。

Knowledge distillation 係 LISA QA 模型、MQM 錯誤分類法、ATA 認證備考。

Self-quality 係每一千字 MQM 錯誤數同文化標記計數。

Surpass-human 係以十倍速度喺 MQM 錯誤率上面打敗 LSP 人類 QA。

Accepts critique 係 NativeReviewerAgent 同 BrandAgent。

Comments on 係 VoiceCloneAgent 嘅發音同 DubbingAgent。

Tools，歷史性嘅，係 DeepL 同 Google Translate API、MQM 錯誤標註器、術語管理即 memoQ API。

Architecture 係 Self-Refine，括號 rubric 係 MQM scoring framework。

第十二部分，相關設計閱讀。

用呢啲嚟加深設計保真度。佢哋唔會覆蓋本地 agent_spec.json。

喺 va-agent-swarm 之下冇強關鍵字匹配，依賴本地 SPEC 同 VA 表格行。

本地知識索引，來自 SPEC，包括以下項目。

Runtime binding 連結到 agent_spec.json。Folder README 連結到 README.md。Provenance 連結到 sources/PROVENANCE.json。Mapping note 連結到 sources/MAPPING.md。Pack inventory 連結到 ../../inventory.json。Pack manifest 連結到 ../../manifest.json。所有必要嘅主要參考資料都喺呢個 repository 入面解析。

Folder README 摘要如下。VA-aligned Domain Pack agent，common host。檔案列表：SPEC.md 嘅用途係 VA/generic depth 加 host binding。agent_spec.json 嘅用途係 Fail-closed runtime。sources/ 嘅用途係 Provenance 同 mapping。

第十三部分，附錄，host 合約即 agent_spec.json。

呢個係 CASOPS 上面呢個 agent 嘅權威 fail-closed 配置。

agent_id 係 video.localizationqa。

allowed_tools 係空列表。

budget_policy 入面，max_input_tokens 係 2048，max_output_tokens 係 1024，max_tool_requests 係 0。

critique_edges 入面，inputs 係 video.critic，outputs 係 video.judge。

max_refinement_count 係 3。

model_policy 入面，model_id 係 local-video-config-v1，network_access 係 false，provider 係 local_deterministic。

production_activation_requested 係 false。

prompt_reference 係 video.prompt.localizationqa.v1。

role 係 LocalizationQAAgent (Linguist) (VA Domain Pack)。

rubric_reference 係 video.rubric.localizationqa.v1。

schema_version 係 1.0。

status 係 registered。

va_id 係 44。

va_name 係 LocalizationQAAgent (Linguist)。

va_category 係 7-Edu。

第十四部分，附錄，SPEC 責任摘錄。

展開嘅責任文字來自本地 SPEC.md。

核心責任係翻譯加文化適配。

Host role binding 係 LocalizationQAAgent (Linguist) (VA Domain Pack)。設計時 VA 表格內容係歷史性嘅，對啟動無約束力。

來自 VA 表格嘅責任係翻譯加文化適配。

歷史知識蒸餾來源係 LISA QA 模型、MQM 錯誤分類法、ATA 認證備考。

歷史自我質量標準係每一千字 MQM 錯誤數同文化標記計數。

歷史超越人類信號係以十倍速度喺 MQM 錯誤率上面打敗 LSP 人類 QA。

歷史 Critique bus：接受批評嘅來源係 NativeReviewerAgent 同 BrandAgent。評論對象係 VoiceCloneAgent 嘅發音同 DubbingAgent。

歷史工具設計時備註，呢啲係非啟動性質嘅：DeepL 同 Google Translate API、MQM 錯誤標註器、術語管理即 memoQ API。

Runtime 安全提醒：Host 允許列表只有 agent_spec.json 加 tool-permission-register.json。CI 用 video_* stubs。唔好將設計時供應商名稱當作已啟用嘅 API。

歷史架構模式係 Self-Refine，括號 rubric 係 MQM scoring framework。

第十五部分，文件控制。

Generator 係 scripts/business/generate_agent_user_guides.py。

Agent folder 係 business/video/agents/video.localizationqa/。

Output 係 docs/user_guide.md。

VA corpus root 係 C:\Project\va-agent-swarm。

Regeneration 命令係 python scripts/business/generate_agent_user_guides.py。

Trust model 係設計語料庫等於不受信任嘅數據，host config 等於 fail-closed。

呢個就係 LocalizationQAAgent (Linguist) 嘅指南嘅完結。從第一部分快照同第五部分幾時用開始閱讀。用第八到第十部分做 CASOPS 操作同改進。只有需要深層設計保真度嘅時候先用第十一到第十二部分。
