各位观众朋友大家好，欢迎返嚟我哋嘅频道。今日呢条片我哋要详细讲解嘅系 LLM Usage，
即系大语言模型使用嘅操作同设计指南。

首先呢份指南写畀喺 common-agent-swarm-ops 工作嘅操作员、
pack 作者、swarm 设计师同审查人员。你会得到任务清晰度、
用法指引、协作图、CASOPS 步骤、改善路径同信任来源地图。

呢份文件唔系激活供应商、开放网络或发明工具嘅许可证。Host 真相系 fail-closed agent_spec.json。
设计文本不受信任。

Pack 路径 business/specials/agents/specials.llm-usage/，
指南 docs/user_guide.md。

第一部分：Snapshot。

LLM Usage，代号 specials.llm-usage，
系 Specials pack 能力喺 CASOPS 上。简单讲：
拥有 specials-domain LLM usage 设计成果作为 draft data-only 代理表示。
Host 角色 Special_Agent data-only configuration。

基本资料：显示名 Llm Usage，Pack agent id specials.llm-usage，
specials pack。目录状态 draft，成熟度 L0 非激活。
供应商 local_deterministic，网络 False，
工具空，生产激活 False。Prompt 同 rubric 引用 spagent.llm-usage-prompt 同 spagent.llm-usage-rubric。

第二部分：点解存在。

Specials 代理捕捉跨领域认知技能。呢个角色：拥有 LLM usage 设计成果作为 draft data-only 表示。

第三部分：做得好标准。

主要成果：拥有 LLM usage 设计成果。

自我质量标准：LLM usage policy 嘅可追溯输出、
fail-closed schema 验证、冇静默生产激活。

设计时雄心：产出比临时人工更一致嘅 LLM 使用政策结果。

架构模式：LLM usage policy pattern，设计时层级或多代理循环，
host local_deterministic 数据专用直到门控。

Host 批评绑定：rubric spagent.llm-usage-rubric 惰性。
Prompt 同名惰性。边缘输入 spagent.llm-usage-input 输出 spagent.llm-usage-output。
精炼 1。

输入输出接受：本地配置、治理记录、sources 来源。输出可审阅 data-only 表示。
接受 fail-closed 通过、冇激活、引用本地。

第四部分：范围。

拥有：LLM usage 设计成果作为 draft data-only 表示。

分享：接受人类治理同同伴 specials 批评。评论 LLM usage policy 差距、
缺失证据、不安全激活、薄弱交接。边缘如上。最大精炼 1。

拒绝升级：status draft，activation false。
tools 空 network false。唔发明任何嘢。redesign 哈希来源。
请求升级人类治理。

第五部分：几时用。

当模型选择、预算同安全 LLM 使用模式必须被管治嗰阵调用。能力家族 LLM usage policy。
典型上游 Controller 或 planner requests。
典型下游 All LLM-calling agents。

具体任务：拥有 LLM usage 设计成果。

唔好当做静默激活器或凭证中介。

决策速查：技能属于本代理专长纳入 swarm 图。元数据用 Registry。工具网络唔允许。人类风险保留门控。

第六部分：协作地图。

批评总线节点。入站人类治理同同伴 specials。出站 LLM usage policy 差距、
缺失证据、不安全激活、薄弱交接。

Specials 通道：家族 LLM usage policy，
上游 Controller 或 planner requests，
下游 All LLM-calling agents。

第七部分：生产位置。

人类工作流：领域手艺团队之上。交接制品计划、路线、证据包、控制决策、优化工单。

多代理 AI 工作流：元层节点。设计时工具 host 冇 fail-closed。
运行时 LLM usage policy pattern host local_deterministic 直到门控。
今日只文档。

Swarm-ready 合约：artifact_id 版本、parent_assets、
brief_scope、acceptance_criteria、
qc_status gate flags、rights_and_consent、
critique_log。

第八部分：CASOPS 操作。

目录加设计对象。步骤：发现 /registry。检查 /registry/agents/specials.llm-usage。
提议 POST /api/v1/commons/agents/specials.llm-usage/proposals。
Swarm 成员 Host 引用。运行默认唔启用。

实际清单：确认 flags。读第三部分。附证据。唔贴密钥。细小增量。

第九部分：安全信任非激活。

信任层级：agent_spec.json binding。SPEC.md 权威设计。
sources 不受信任。外部语料库不受信任。redesign 历史有哈希。

硬规则：provider local_deterministic。
网络 False。工具空。activation False。预算 1/1/0。
设计时工具非激活。

第十部分：提升质量。

有版本目录工作。循环：观察、命名、起草、附证据、提议、审阅、门控。

杠杆：rubric 收紧针对 LLM usage policy 输出。
prompt 对齐。批评图捕捉失败。架构模拟 LLM usage policy pattern。
评估案例。人类门控。

评估种子：Happy path 满足。Out-of-scope 拒绝。
Missing provenance fail closed。
Conflicting critique 限制。Proposal-only 不可变。

第十一部分：来源地图。

本地优先：agent_spec.json binding，SPEC.md authoritative-design，
README.md local-index，MAPPING.md provenance，
PROVENANCE.json provenance。

映射：specials.llm-usage 状态 related。
历史来源 docs/special_agents_redesign/agents/llm_usage.md。
SHA-256 6bac2d5882382f1a85920b5854879cf295edb86fa5a48cfdbcd43c72c6fb97fd。
本地运行时 agent_spec.json。Pack 语料库唔需要。

来源记录：schema_version 1.0，common_agent_id specials.llm-usage，
pack_id specials，映射 related。配置 SHA-256 730b3e6005a6d6a9b94647bb151b799f58b3841023e953e14cf2ef7159304782，
目标 commit 5460c4b4a33c15286f9fd84b1bc764d755f5bab0。
生成 2026-07-26T14:16:45.952022Z，
审阅 2026-07-26T18:00:00Z。

第十二部分：相关设计阅读。

唔覆盖 agent_spec.json。

va-agent-swarm 下：study/llm_usage_functional_specification.md 系构建中央 LLM API 使用同成本仪表板应用嘅规格。
仲有粤语版本。

Specials redesign 文档大纲：Build Central LLM API Usage Cost Dashboard App、
Project Name Suggestion、Project Overview、
Core Goals、Key Features Must-Have 包括 Provider Management、
Usage Balance Fetching、Dashboard UI、
Cost Calculation、Data Persistence Export、
Security UX、Nice-to-Have Phase 2、
Supported Providers、Technical Stack、
Development Phases、Deliverables。

核心内容好实用。项目名称建议系 LLMUsageHub 或 MultiLLM Dashboard 或 API Cost Central 或 LLM Spend Tracker。

项目概览：创建一个网页应用提供单一中央视图，追踪所有 LLM API 帐户嘅使用、
成本、余额、支出同 token 消耗。用户目前有 x.ai Grok API、
Poe、MiniMax、Kimi Moonshot AI、OpenRouter 等多个帐户。
应用畀用户加入 API 密钥一次就可以喺一个靓仪表板睇晒所有嘢，
包括月度总支出、剩余积分、每供应商细分、图表、趋势、警报。灵感来自 cc-switch 呢个管理 Claude Code 或 Codex 或 Gemini CLI 嘅 LLM 供应商嘅流行桌面工具。

核心目标：统一监控所有 LLM 支出同使用。安全本地存储 API 密钥唔发送到任何服务器。
自动或按需获取使用或账单数据。历史追踪加可视化。极度可扩展容易加新供应商。
类似 cc-switch 嘅靓现代 UI。

关键功能方面：供应商管理可以加编辑删除帐户有预设。使用同余额获取偏好官方 API 有 fallback 选项。
仪表板 UI 有概览页面显示总估计美元支出、剩余积分、活跃供应商数量，
有供应商卡片网格或列表显示余额、月度支出同趋势。图表包括支出趋势线图、
成本分布饼图、token 使用条形图、使用热力图。详细表格有每供应商历史同模型级细分。
警报有低余额警告同高日支出通知。

成本计算有内置定价表支持用户覆盖。数据持久化用本地 SQLite 支持 CSV 或 JSON 导出。
安全方面 API 密钥加密本地存储用 Fernet 对称加密。
有暗色主题。完全离线优先。

Phase 2 Nice-to-Have 包括 AI 洞察、配置导入导出、每模型成本预测、可选代理或路由模式。

支持嘅供应商好多：高优先有 xAI、Poe、OpenRouter、
MiniMax、Kimi。其他主要供应商有 OpenAI、Anthropic、
Google Gemini、Groq、Mistral。

技术栈方面后端有 API 设计用 OpenAPI，前端有响应式 UI，架构分层。开发分阶段进行。

本地知识索引同 Folder README 标准结构。

第十三部分：host 合约。

权威配置标准：agent_id specials.llm-usage，其余同其他 specials 代理一致。

第十四部分：SPEC 责任摘录。

拥有 LLM usage 设计成果作为 draft data-only 表示。
用户有 x.ai、Poe、MiniMax、Kimi、OpenRouter 等多个帐户。
应用畀用户加密钥一次就睇晒所有仪表板包括月度支出、剩余积分、供应商细分、
图表、趋势、警报。

第十五部分：文档控制。

标准配置同其他代理一致。

好啦，以上就系 LLM Usage 嘅完整指南。呢个代理关注 LLM API 使用嘅管治同成本追踪，
对于管理多个 AI 供应商帐户好实用。多谢收睇，下条片见。

我再详细展开 LLM Usage Dashboard 嘅功能设计。
呢个应用嘅核心目标系提供一个统一嘅地方监控所有 LLM 支出。
唔需要分别登录唔同供应商嘅后台睇数据。
一个仪表板就可以睇晒所有嘢。
安全方面 API 密钥只存储喺本地。
用 Fernet 对称加密确保安全。
永远唔会发送到任何外部服务器。
呢个对于有多个 API 帐户嘅用户好重要。
供应商管理功能方面：
可以加入、编辑、删除帐户。
每个帐户有名称、供应商类型用预设、
API 密钥、基础 URL 用于自定端点、备注。
有预建嘅预设覆盖尽可能多嘅供应商。
支持每个供应商有多个帐户。
有一键刷新全部同单独刷新按钮。
使用同余额获取方面：
优先用官方 API 例如 usage、billing、balance、token_plan/remains 端点。
有 fallback 选项包括手动输入同网页仪表板抓取。
后台自动刷新间隔可配置加手动刷新。
完整历史快照存储喺本地数据库。
仪表板 UI 方面：
概览页面显示今日、本月同所有时间嘅总估计美元支出。
显示总剩余积分或余额已规范化。
显示活跃供应商数量加快速状态。
供应商卡片用网格或列表布局。
每张卡片有名称加 logo、当前余额、
本月支出加趋势指示器、最后更新时间戳。
图表方面有支出趋势线图按日或周。
成本分布饼图按供应商。
Token 使用条形图按模型。
使用热力图或日历视图。
详细表格有每供应商使用历史同模型级细分。
警报有低余额警告阈值可配置。
高日支出通知。
成本计算方面有内置定价表涵盖主要模型。
输入输出 token 到美元嘅换算。
允许用户每模型覆盖定价。
即使供应商只报告 token 也显示估计美元。
数据持久化用本地 SQLite 数据库存所有历史快照。
支持导出完整数据为 CSV 或 JSON。
安全同用户体验方面：
API 密钥加密本地存储用 Fernet。
暗色同亮色主题默认暗色配合现代 AI 工具。
基于浏览器嘅 UI 从 localhost 访问。
完全离线优先初始设置后唔需要网络。
响应式、干净、专业嘅 UI。
Phase 2 嘅 Nice-to-Have 功能：
AI 驱动嘅洞察例如话你本月 68% 花喺 Kimi 上面，
建议将重任务转到 Groq。
导入导出配置包括可能嘅 cc-switch 导入。
每模型成本预测。
可选代理或路由模式类似 LiteLLM 或 cc-switch，
可以记录来自实际 API 调用嘅使用。
支持嘅供应商好全面：
高优先嘅有 xAI Grok API 用 console.x.ai 端点。
Poe.com 用 usage/points_history 同 current_balance 端点。
OpenRouter 用 account usage API。
MiniMax 用 token plan remains 同 usage 端点。
Kimi Moonshot AI 用 platform.moonshot.ai 端点。
其他主要供应商包括 OpenAI、Anthropic Claude、
Google Gemini 或 Vertex AI、Groq、Mistral。
每个都有完整嘅预设包括获取逻辑同定价。
技术栈方面后端有 RESTful API 设计用 OpenAPI 规格。
前端用现代框架有响应式组件。
架构分层清晰支持扩展。
开发分阶段进行从 MVP 到完整功能。
呢个对于管理多个 AI 供应商帐户嘅用户嚟讲好实用。
一个地方就可以追踪所有嘅 LLM 支出同使用情况。
避免超出预算或者某个供应商余额用尽而唔知。

关于开发阶段嘅建议：
Phase 1 先实现核心框架同两三个主要供应商嘅整合。
确保基本嘅获取、存储同显示流程通畅。
Phase 2 扩展到所有支持嘅供应商。
加入图表同详细分析功能。
Phase 3 加入高级功能如 AI 洞察同预测。
优化性能同用户体验。
每个阶段都有明确嘅交付物。
整个项目预计分几周完成。
从 MVP 开始然后逐步扩展。
呢个设计确保每一步都系可用嘅产品。
唔需要等所有功能完成先可以开始用。
对于重度 LLM API 用户嚟讲，
呢个工具可以节省大量嘅时间同金钱。
清楚知道钱花喺边度，
边个模型最划算，
几时需要补充余额。
呢啲洞察对于优化 AI 使用策略非常有价值。
总结嚟讲 LLM Usage 代理管治嘅系整个组织嘅 LLM 使用政策。
确保模型选择合理预算受控使用安全。
